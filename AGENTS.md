# 🤖 AGENTS.md — الملف التوجيهي الشامل لمشروع Mr Center

> **هذه الوثيقة مكتوبة لوكيل الذكاء الاصطناعي الذي سيعمل على هذا المشروع.**
> اقرأها كاملة قبل لمس أي سطر. هي المرجع النهائي لفهم المشروع من أوله لآخره:
> ماذا يريد المالك، كيف بُني، لماذا اتُّخذت كل قرار، وما الذي يُمنع كسره.
>
> آخر تحديث: سبتمبر 2026 — الحالة: مشروع مكتمل ومُختبر (392 فحصاً آلياً ناجحاً + اختبار حي E2E).

---

## جدول المحتويات

1. [الهوية والرؤية](#1-الهوية-والرؤية)
2. [ما يريده المالك حرفياً (متطلبات مُلزِمة)](#2-ما-يريده-المالك-حرفيا-متطلبات-مُلزِمة)
3. [الخريطة المعمارية الكاملة](#3-الخريطة-المعمارية-الكاملة)
4. [نموذج تعدد السناتر والصلاحيات](#4-نموذج-تعدد-السناتر-والصلاحيات)
5. [قاعدة البيانات بالتفصيل](#5-قاعدة-البيانات-بالتفصيل)
6. [نظام كلاود فلير (الوسيط المركزي)](#6-نظام-كلاود-فلير-الوسيط-المركزي)
7. [بنية كود التطبيق ملفاً ملفاً](#7-بنية-كود-التطبيق-ملفاً-ملفاً)
8. [تدفقات الاستخدام الحرجة](#8-تدفقات-الاستخدام-الحرجة)
9. [الثوابت التي يُمنع كسرها](#9-الثوابت-التي-يُمنع-كسرها)
10. [الاختبارات والتحقق](#10-الاختبارات-والتحقق)
11. [البناء والتوزيع والتحديثات](#11-البناء-والتوزيع-والتحديثات)
12. [علاقة التطبيق بموقع الويب القديم](#12-علاقة-التطبيق-بموقع-الويب-القديم)
13. [بيئة العمل وخبرات عملية مهمة](#13-بيئة-العمل-وخبرات-عملية-مهمة)
14. [قيود معروفة وخطط مستقبلية](#14-قيود-معروفة-وخطط-مستقبلية)
15. [قاموس مصطلحات المالك](#15-قاموس-مصطلحات-المالك)
16. [قائمة فحص قبل التسليم](#16-قائمة-فحص-قبل-التسليم)

---

## 1) الهوية والرؤية

- **المنتج**: تطبيق جوال لإدارة «السناتر» التعليمية المصرية — طلاب (متعددو المجموعات)، مجموعات بمدرسين وتسعير متنوع، حضور (يدوي + QR)، مدفوعات ومستحقات، درجات يدوية والكترونية، اختبارات بأنواع، تقارير PDF، واتساب، إشعارات داخلية وفورية، استبيانات، طلبات، مكتبة (شرف/ملفات/روابط)، جدول أسبوعي، فريق عمل بصلاحيات، باقات واشتراكات.
- **التقنية**: React Native + Expo SDK 52 + TypeScript + expo-router v4. يعمل على أندرويد الآن، وعلى iOS لاحقاً بنفس الكود.
- **اللغة**: واجهة عربية 100% باتجاه RTL مفروض (`I18nManager.forceRTL` + `direction:'rtl'` على أشرطة التبويبات)، وتعليقات الكود والتوثيق بالعربية. المالك يتكلم عربية (لهجة مصرية) — خاطبه بها.
- **الهوية البصرية**: «كربون المستقبل» — داكن فحمي + نعناعي كهربائي `#00E5A0` + كهرماني، مع أنيميشن ضغط (spring) على الأزرار والبطاقات. **داكن فقط — لا يوجد وضع فاتح** (يتطلب إعادة بناء كل شاشة).
- **القاعدة**: Supabase (PostgreSQL + Auth + RLS) — **نفس مشروع Supabase الذي يستخدمه موقع الويب الحالي للمالك**، بحيث تظهر البيانات متطابقة بين التطبيق والموقع لأنهما على نفس الجداول.
- **الوسيط**: عامل Cloudflare (Worker) هو نقطة التحكم المركزية لمفاتيح القاعدة وللتحديثات وللدفع الفوري (راجع القسم 6).

### المستودعات المرتبطة
| المستودع | الدور |
|---|---|
| `contashepo-create/Mr-center-android-` (هذا) | تطبيق الجوال + مخطط SQL + عامل كلاود فلير + الأدلة |
| `contashepo-create/centerpuplish` | موقع الويب الحالي (Next.js + Supabase، أحادي المستأجر قديماً) — سيُرقَّى لاحقاً ليواكب هذا المخطط |

- فرع العمل في هذا المستودع: **`arena/01a08697-mr-center-android`** — اعمل عليه فقط وادفع إليه فقط.

---

## 2) ما يريده المالك حرفياً (متطلبات مُلزِمة)

هذه ليست اقتراحات — هي مواصفة المنتج. أي تغيير مستقبلي يجب أن يحافظ عليها:

1. **تطبيق أندرويد كامل احترافي** يعمل على كل الأجهزة وأحجام الشاشات، بهوية مستقبلية وأنيميشن سلس، وشاشات لكل قسم.
2. **الربط بنفس قاعدة Supabase المربوطة بالموقع** — حتى لو أُغلق الموقع لأي سبب، يعمل التطبيق مستقلاً بالكامل.
3. **نظام صلاحيات من أربع فئات**:
   - **سوبر أدمن = المطور = المالك**. الوحيد الذي تظهر له لوحة تحكم المطور.
   - **صاحب السنتر** (`center_admin`): صلاحيات كاملة داخل سنتره فقط.
   - **فريق العمل** (`teacher`/`manager`/`secretary`): يسجلون بكود السنتر، وحساباتهم **خاملة حتى يفعّلها المالك**، ويعملون داخل **صلاحيات مفصلة (10 مفاتيح)** ومجموعات مسندة فقط. **المنع هو الافتراضي** — كل ما لم يُمنح صراحة محجوب.
   - **طالب**: يرى بياناته فقط (حضوره، درجاته، مدفوعاته، إعلانات وإشعارات سنتره).
4. **عزل كامل بين المستخدمين بعمود `center_id`** — سنتر لا يرى بيانات سنتر آخر إطلاقاً، والطالب لا يرى إلا سجلاته هو (مثبت باختبار حي E2E على القاعدة الحقيقية).
5. **كود السنتر**: يختاره صاحبه عند التسجيل من 3–8 حروف أو أرقام (إنجليزي/عربي/مختلط)، **ثابت لا يتغير** و**فريد على مستوى النظام**؛ رسالة «الكود غير متاح» إن كان مأخوذاً، وتنبيه مسبق أن الكود ثابت وفريد. + **باركود سنتر ثابت** للطباعة على المكتب (يحمل الكود والاسم) يُمسح بدل الكتابة.
6. **تسجيل الطالب بكود السنتر مرة واحدة فقط** — يرى اسم سنتره للتأكيد، ويختار **صفه ومجموعته من قوائم**، ويُربط بـ `center_id` تلقائياً؛ **عند الدخول لاحقاً لا يُطلب الكود أبداً**. الطالب قد ينضم **لأكثر من مجموعة** (مواد/مدرسون مختلفون) — من التسجيل أو من ملفه لاحقاً.
7. **الحسابات فريدة عالمياً**: البريد ورقم الهاتف لا يتكرران في النظام كله — «الإيميل مستخدم» / «الرقم مستخدم». البريد من **موفرين معروفين فقط** (Gmail/Yahoo/Outlook/...) — المؤقتة مرفوضة. **رقم ولي الأمر إجباري ومختلف عن رقم الطالب** (واجهة + خادم).
8. **نوعا الحسابات**: **سنتر متكامل** (صلاحيات كاملة + فريق تابع — اشتراكه أعلى) و**مدرس خصوصي مستقل** (`solo`: نفس الوظائف لمواده وطلابه بكوده، بلا فريق، حتى 200 طالب).
9. **الصفحة الرئيسية**: دخول كبير (مسئول/طالب) + تسجيل صغير (سنتر/طالب) + دخول/انضمام فريق العمل + حول التطبيق.
10. **لوحة تحكم المطور مخفية تماماً** — ضغطة مطوّلة (٥ ثوانٍ) على الشعار أو ٥ نقرات فوتر + دخول حساب المطور.
11. **من لوحة المطور**: مفاتيح القاعدة لكل العملاء · باقات احترافية واعتماد طلبات الترقية · إيقاف/تفعيل أي سنتر · ملف أي سنتر كامل (مالك/فريق/طلاب/اشتراكات/نشاط) · بث للجميع/الأصحاب/سنتر مع تتبع القراءة · محرر «حول التطبيق» والرسالة العامة.
12. **الباقات**: تجريبية ٧ أيام بمزايا كاملة → شاملة (600/6500/12000: مدير+2 سكرتير+4 مدرسين) / متوسطة (400/4500/8500: 1+1+2) / خصوصي (300/3000/5000: حتى 200 طالب). الطلب من صفحة الباقات (خطة+مدة+تاريخ وقيمة التحويل) ويعتمده المطور. الحدود **مفروضة خادمياً** (مشغّل + عدّ).
13. **مفاتيح قاعدة البيانات عبر كلاود فلير كوسيط** + **التحديث الذاتي** + **الدفع الفوري والتذكير** عبر العامل (راجع القسم 6).

---

## 3) الخريطة المعمارية الكاملة

```
┌──────────────────────────┐     ┌──────────────────────────┐
│      تطبيق الجوال        │     │      موقع الويب          │
│  (Expo / React Native)   │     │  (Next.js — سيُرقَّى)    │
└───────────┬──────────────┘     └───────────┬──────────────┘
            │                                │
            │  ① عند كل إقلاع:               │ يتصل مباشرة
            │  GET <configUrl>               │ بـ Supabase
            ▼                                │ (بمفاتيحه الخاصة)
┌──────────────────────────┐                 │
│   عامل Cloudflare Worker │                 │
│  /config: database+update │                 │
│  /push/notify + cron: دفع │                 │
└───────────┬──────────────┘                 │
            │ ② عميل Supabase                │
            ▼                                ▼
            ┌──────────────────────────────────────────────┐
            │   Supabase (PostgreSQL + Auth + RLS)         │
            │   نفس المشروع = نفس الجداول = بيانات متطابقة │
            │   العزل والأمان على الخادم عبر RLS           │
            └──────────────────────────────────────────────┘
```

**مبدأ حاسم**: التطبيق لا يخزن أسراراً. المفاتيح من كلاود فلير، والأمان كله RLS — فمفتاح anon الظاهر لا يمنح شيئاً تمنعه السياسات. مفتاح `service_role` لا يظهر في التطبيق إطلاقاً (للعامل وسكريبتات الإدارة فقط، ويُبدَّل بعد أي مشاركة).

---

## 4) نموذج تعدد السناتر والصلاحيات

### الجداول الجديدة (أنشأها مخططنا)
| الجدول | الغرض | أعمدة مفتاحية |
|---|---|---|
| `centers` | السناتر | `id` uuid · `name` · `code` (فريد بـ upper، ثابت) · `kind` ('center'/'solo') · `owner_*` · `status` |
| `profiles` | كل مستخدم Auth ودوره | `id`=auth.uid · `role` (6 أدوار) · `center_id` · `student_id` · `is_active` (الفريق خامل حتى التفعيل) · `perms` (10 صلاحيات jsonb) · `push_token` |
| `student_groups` | عضويات الطالب الإضافية | `PRIMARY KEY(student,group)` + عدّاد المجموعة يشمل الكل |
| `teacher_groups` | إسناد المجموعات للفريق (نطاق عمله) | `PRIMARY KEY(teacher,group)` |
| `center_subscriptions` | الاشتراكات | `plan_type` (trial/monthly/yearly/custom/center_full/center_medium/solo_teacher) · `starts_on/ends_on` · `status` |
| `subscription_requests` | طلبات الترقية | `plan/months/amount/transfer_at` · `status` (pending/approved/rejected) — كتابة المالك pending فقط |
| `activity_log` | سجل عمليات لا يُحذف | كتابة الفريق المفعّل، قراءة المالك والمطور |
| `app_exams`/`app_exam_attempts` | اختبارات (mcq/tf/essay بدرجات) + محاولة واحدة (التصحيح عبر RPC) | `questions/answers` jsonb · `status` (graded/pending_review) · `UNIQUE(exam,student)` — **بلا إدخال مباشر للطالب عمداً** |
| `app_inquiries` | طلبات (سؤال/نقل/تسجيل) + رد وحالة | `kind` · `status` · `reply` |
| `app_surveys`/`app_survey_responses` | استبيانات + رد واحد | `is_active` · `UNIQUE(survey,student)` |
| `center_settings` | إعدادات تشغيلية | `settings` jsonb (واتساب/فتح التسجيل/سنة الأرشيف) |
| `app_notifications`/`app_notification_reads` | بث داخلي (صف/رسالة) + مقروءية | `audience` (all/grade/group/student/**owners**) · `UNIQUE(notification,student)` — الطالب عبر RPC مفلتر فقط |
| `app_config` | عامة | `'public_config'` (قراءة عامة، كتابة مطور) · `'legacy_admins'` (القائمة البيضاء) |

### جداول الموقع القديم الموسَّعة
`grades` · `groups` (+`teacher_name/phone` + تسعير `billing_type/weekly_price/session_price`) · `students` (+`guardian_phone`) · `dues` · `payments` · `sessions` · `attendance` · `announcements` · `manual_grades` · `exams` · `honorees` (+`student_id/group_id`) · `shared_files` · `important_links` — كلها `center_id` (القديم NULL ويخص الموقع فقط).

### قواعد العزل (RLS)
- **دوال** `my_role/my_center_id/my_student_id/is_legacy_admin/center_is_active/admin_owns_center/teacher_is_active/teacher_center_ok` (`SECURITY DEFINER`).
- **الطالب**: قراءة نفسه فقط (`my_student_id`) + سنتره العام (`my_center_id`) + عضوياته + RPC الامتحانات/الإشعارات. لا كتابة إطلاقاً.
- **الفريق المفعّل** (`teacher/manager/secretary`): قراءة سنتره + كتابة (حضور/درجات/اختبارات/إعلانات/استبيانات/شرف/طلبات-رد/إشعارات/تحصيل) — والواجهة تحجب فوق ذلك بالصلاحيات العشر (`can()` + `NoAccess` + تبويبات مخفية + منع افتراضي للقوائم).
- **المسئول**: كامل على سنتره الفعّال + إدارة فريقه (تفعيل/صلاحيات/حذف عبر `profiles_teacher_manage/_delete` والمشغّل).
- **المطور/النظام القديم**: كامل. **الزائر**: `public_config` + صفوف NULL القديمة + `lookup/check_availability/signup_lists`.

### دوال RPC
| الدالة | الأذونات | الوظيفة |
|---|---|---|
| `get_published_exams()` | auth | امتحانات السنتر بلا إجابات + هل حاولها |
| `submit_exam_attempt(exam,answers)` | auth | تصحيح (mcq/tf بدرجات، مقالي→مراجعة) + منع التكرار |
| `lookup_center_by_code(text)` | anon+auth | `{id,name,owner_name,status}` — يميز الموقوف (DROP قبل تغيير الشكل) |
| `check_registration_availability(email,phone)` | anon+auth | `{email_taken, phone_taken}` |
| `get_center_signup_lists(uuid)` | anon+auth | أسماء الصفوف/المجموعات للسنتر الفعّال فقط (للتسجيل) |
| `complete_center_registration(5 args)` | auth | سنتر+ملف+تجريبي 14 يوماً (`trial`) + نوع الحساب |
| `complete_student_registration(6 args)` | auth | بوابة التسجيل + ربط بسجل موجود + صف/مجموعة + سقف 200 للمنفرد + رقم ولي مختلف |
| `register_staff_account(center,name,phone,role)` | auth | حساب فريق خامل (يرفض المنفرد `staff_not_allowed`) |
| `get_my_notifications()` | auth | إشعارات الطالب مفلترة + مقروءية |
| `get_my_subscription()` | auth | `{status, plan_type, ends_on, days_left, center_status}` (سوبر → active) |

### المشغلات والقيود
- `trg_guard_center_code`: الكود وstatus للمطور فقط. `trg_guard_profile_identity`: الهوية للمطور + استثناء إدارة المسئول لفريقه. `trg_sync_group_count(+junction)`: عدّاد المجموعة (أساسية+إضافية). `trg_staff_limit_check`: حدود الباقة خادمياً (`staff_limit_reached`).
- فرادة: `centers_code_unique` · `profiles_email_unique` · `profiles_phone_unique` · `UNIQUE(exam,student)` · `UNIQUE(survey,student)` · `UNIQUE(notification,student)`.
- **تحذير النسخ**: أي دالة يُغيَّر توقيعها/نوع إرجاعها تُسقط بـ `DROP FUNCTION` أولاً (خطأ 42P13)، والدوال قبل السياسات التي تستخدمها (خطأ 42883) — ويفحصهما `audit-sql` آلياً.

---

## 5) قاعدة البيانات بالتفصيل

ملف المخطط الوحيد: **`supabase/android_multitenant_schema.sql`** — آمن لإعادة التشغيل (IF NOT EXISTS / OR REPLACE / DROP IF EXISTS) داخل معاملة واحدة، لا يحذف بيانات ولا يكسر الموقع. يبذر `public_config` و`legacy_admins` من `auth.users` لحظة الترحيل. **لا تُعِد تشغيل مخطط الموقع القديم بعده أبداً.**

إعدادات Auth: «Allow new users to sign up» + **«Confirm email» مفعّل** (لمنع الوهمي). التسجيل على مرحلتين: `signUp` ← معلق (`pendingRegistration.ts`: سنتر/طالب/فريق) ← تأكيد البريد ← أول دخول يستكمل تلقائياً (`LoginForm` + مطابقة النوع مع الشاشة + الاحتفاظ بالمعلق عند الفشل). البريد من موفرين معروفين فقط (فحص عميلي بقائمة) ورقم الولي إجباري ومختلف (عميل + خادم `same_guardian_phone`).

صلاحيات الجداول: `GRANT ... TO anon, authenticated, service_role` (الأخير لعمليات الخادم فقط — العامل والسكريبتات — ويُبدَّل بعد أي مشاركة).

إنشاء المطور:
```sql
INSERT INTO public.profiles (id, role, full_name, email)
VALUES ('<UID>', 'super_admin', 'المطور', 'dev@mail.com');
```

---

## 6) نظام كلاود فلير (الوسيط المركزي)

### العامل — `cloudflare/worker.js`
يعدّله المالك من اللوحة ثم Deploy: `DATABASE` (المفاتيح) · `UPDATE` (الإصدار/APK/الإجبار). المسارات: `/config` (و`/`) · `/version` · `/database` · `/push/notify` (POST + `x-push-secret`) · CORS مفتوح + كاش 60 ثانية.

### الدفع الفوري والتذكير (اختياري — مجاني)
- متغيرات Secrets مشفرة: `SUPABASE_URL` + `SUPABASE_SERVICE_KEY` + `PUSH_SECRET` + `TZ_OFFSET_MINUTES` (مصر 120/180).
- Cron كل 15 دقيقة → تذكير الحصص (مجموعات تبدأ خلال ~ساعة، تشمل العضويات الإضافية) حتى والتطبيق مغلق.
- Webhook عند INSERT في `app_notifications` → `/push/notify` (الدليل في `cloudflare/README.md`).
- رمز الدفع يُسجل صامتاً عند الدخول (`push_token`) — يتطلب معرف مشروع EAS في نسخة المتجر.

### التهيئة والتحديث
سلسلة `initSupabase()`: يدوي → كلاود فلير (مع كاش آخر رد) → مخزن → مدمج → شاشة `ConnectionSetup`. الثابت الوحيد `extra.configUrl`. التحديث الذاتي عبر R2 + `UPDATE` (`REQUEST_INSTALL_PACKAGES`)، يتخطى DEV/Expo Go/غير أندرويد.

---

## 7) بنية كود التطبيق ملفاً ملفاً

```
index.ts                  فرض RTL ثم expo-router/entry
app.json                  com.mrcenter.app · CAMERA + plugins (camera recordAudio=false) · extra.configUrl
eas.json                  preview=APK · production=AAB (autoIncrement)

app/  (مجلدات (x) بلا مسار)
├─ _layout.tsx            الجذر: SessionProvider + RouterGuard (أدوار/اشتراك/إيقاف/موقوف) + <UpdateManager/> (تلاشي fade بخلفية داكنة)
├─ index.tsx              الترحيب: دخول كبير + تسجيل صغير + فريق العمل + حول + بوابة المطور (5 ثوانٍ/5 نقرات) + ConnectionSetup
├─ about.tsx              حول: محتوى المطور + قائمة كل المميزات + تواصل
├─ blocked.tsx            حظر الاشتراك (حالة/باقة/تواريخ معربة + واتساب + إعادة فحص)
├─ auth/                  login-admin/student/teacher (أخيرة للفريق) · register-center (نوع الحساب center/solo + ملاحظة الأسعار) · register-student (كود/باركود + صف/مجموعة) · register-teacher (فريق: مدرس/مدير/سكرتير)
├─ developer/             بوابة لكل صفحة (DeveloperGate) + index (إحصائيات تشمل الفريق) + connection (خروج إجباري عند تبديل القاعدة) + broadcast (تجميع البثوث + معاينة + تتبع) + centers (واتساب المالك + تفاصيل) + center-detail (ملف السنتر الكامل) + subscriptions (منتجات + اعتماد طلبات) + app-info
├─ (admin)/               تبويبات (إخفاء حسب صلاحيات الفريق + شارة اشتراك للمالك فقط)
│  ├─ dashboard.tsx       ترحيب + (اشتراك للمالك فقط) + إحصائيات + إجراءات مفلترة بالصلاحية
│  ├─ students.tsx        بحث معقم + فلتر حالة (نشط/موقوف/مؤرشف) ومجموعات + بطاقات منفصلة + (إدارة للمالك فقط)
│  ├─ groups.tsx          CRUD (للمالك) + TimePicker + تسعير (شهري/أسبوعي/بالحصة) + مدرس + بطاقات غنية + تحقق أيام/زمن + تنبيه تعارض
│  ├─ attendance.tsx      تنقل تاريخ بسقف اليوم + كشف بلا حصص وهمية (تُنشأ عند الحفظ) + حاضر/متأخر/غائب + كل حاضر/غائب + ماسح
│  ├─ scan.tsx            كاميرا بلا أطفال (طبقة عائمة) + إذن تلقائي + إعدادات عند الرفض + بحث يدوي + رفض غير النشط + تحصيل بالأقدم
│  ├─ teachers.tsx        الفريق: فلتر أدوار + حدود الباقة + تفعيل/إيقاف/حذف + 10 صلاحيات + إسناد مجموعات
│  ├─ subscription.tsx    الباقات (للمالك فقط): خطته + المنتجات + طلب ترقية (خطة/مدة/تاريخ وقيمة التحويل) + سحب الطلب
│  ├─ activity.tsx        سجل العمليات (للمالك فقط)
│  ├─ payments.tsx        توليد حسب التسعير (طالب+مجموعة) + معلق/جزئي(Dفع ناقص)/مسدد + سجلات الأيتام بلا تنقل
│  ├─ exams.tsx           (اختياري/صح-خطأ/مقالي بدرجات): قوالب + معاينة + منع نشر ناقص + تحذير تعديل المنشور + نتائج + تصحيح يدوي + PDF
│  ├─ inquiries.tsx       فلتر + رد (رد/قبول/رفض) + إغلاق + فتح ملف الطالب للنقل
│  ├─ surveys.tsx         إنشاء + تفعيل + نتائج + قفل الأسئلة المُجابة (فحص لحظي)
│  ├─ library.tsx         شرف (اختيار طالب مفلتر + اسم يدوي للأرشيف) + ملفات + روابط (تحقق URL)
│  ├─ schedule.tsx        أسبوعي + تعارض + مجموعات بلا أيام + أسعار حسب التسعير + PDF
│  ├─ reports.tsx         شهر: حضور/تحصيل/متوسطات (طالب محذوف موسوم) + PDF
│  ├─ whatsapp.tsx        نطاق (كل/صف/مجموعة/طالب) + هدف (طالب/ولي) + قوالب + إرسال منفصل (فشل رقم لا يعطل) + نطاق المدرس لمجموعاته
│  ├─ notifications.tsx   بث (عدّ دقيق بعضويات إضافية) + مقروءية باستعلام واحد + حذف
│  ├─ dev-notices.tsx     صندوق المطور (للمالك فقط)
│  ├─ announcements.tsx   بطاقات (مثبت مميز + عرض كامل) + مودال حديث
│  ├─ grades-list.tsx     إضافة + تعديل + حذف مع فك الارتباط (عرض فقط للفريق)
│  ├─ guide.tsx           دليل محدث (فريق/باقات/واتساب/اختبارات)
│  ├─ admin-settings.tsx  بيانات + اشتراك (planLabel) + تشغيلية (تحقق واتساب/بريد) + نسخ احتياطي كامل + (محجوب عن الفريق)
│  ├─ more.tsx            أقسام + كود + باركود السنتر (تصدير PNG باسم لاتيني) + روابط (فريق/إعدادات للمطور... للمالك فقط)
│  └─ student/[id].tsx    ملف شامل: مجموعات (أساسية+إضافية للمالك فقط) + مستحقات/دفعات (تحصيل collect) + درجات (grades) + محاولات بعناوين + سجل نشاط + تقرير PDF شامل + واتساب (notify)
└─ (student)/             تبويبات + شاشات مخفية (my-exams/my-inquiries/my-surveys/my-library/my-schedule/my-notifications)
   ├─ home.tsx            ترحيب + مجموعتي + باركود يومي + أوائل + شارة غير المقروء + روابط + إعلانات (20)
   ├─ my-exams.tsx        منشورة + تأدية كل الأنواع بمؤقت + خروج مؤكد + أسئلة معطوبة موسومة + نتائج (مراجعة)
   ├─ my-notifications.tsx غير مقروء/الأحدث + تعليم عند الفتح (UNIQUE يمنع التكرار)
   ├─ my-schedule.tsx     كل مجموعاته (أساسية+إضافية)
   ├─ my-library/surveys/inquiries/attendance/grades/payments/profile (اسم السجل أدق + تحميل آمن)

src/
├─ theme.ts               هوية «كربون المستقبل» (فحمي+نعناعي+كهرماني — داكن فقط) + ظلال
├─ components/            layout (BackHeader برجوع آمن canGoBack) · controls (AppButton/Content بأنيميشن spring + تباين أحبار + NoAccess + SheetHandle) · pickers (OptionPicker + DaysPicker + TimePicker + FormMessage بزر نسخ) · LoginForm (أدوار ثلاث + معلق + إعادة تأكيد + نسيت محمي) · DeveloperGate (صفحة لا Layout) · ConnectionSetup · UpdateManager (تباين داكن)
└─ lib/
   ├─ types/api/supabase/session/config/pendingRegistration (سنتر/طالب/فريق)
   ├─ rbac.ts             المنطق النقي القابل للاختبار في Node: STAFF_ROLES + isStaff/isOwner + can() + roleLabel + TEACHER_TABS/SCREENS
   ├─ staff.ts            غلاف يعيد تصدير rbac + useTeacherGroupIds (Hook نطاق مجموعات المدرس)
   ├─ billing.ts          PRODUCTS (الأسعار والحدود) + planLabel + limitsFor + priceFor + TRIAL_DAYS=14
   ├─ qr.ts               يومي MRC1 + ثابت MRC0 (XOR+FNV + UTF-8 عربي + يومية تمنع السكرين)
   ├─ whatsapp.ts         wa.me + تطبيع مصري + قوالب
   ├─ push.ts             رمز الدفع صامتاً (بعد فحص projectId أولاً حتى لا يزعج)
   ├─ backup.ts           نسخة JSON كاملة (23 جدولاً) بالتوازي + مشاركة
   └─ report.ts           HTML عربي → PDF ومشاركة

scripts/  test-fresh (منظومة مستقلة: باقات/مصفوفة صلاحيات/بوابات شاشات/ثيم حي/بث مجمّع/rate limit/تنقل/عزل api/مخطط) · test-utils (utils+billing) · test-qr · test-worker (يشمل /push/notify 403/400) · audit-sql · audit-rpc (تطابق وسائط التطبيق↔القاعدة) · audit-app · test-e2e-live (حي: E2E_* keys + تنظيف + تدوير service_role بعده)
```

**اصطلاحات**: `getSupabase()` فقط · لا حالة خارج SessionProvider · الأدوار من `profiles` · أخطاء معربة · RLS أولاً والواجهة تجميل · أي شاشة جديدة تُسجل في (الحارس + التبويبات + audit-app) معاً.

---

## 8) تدفقات الاستخدام الحرجة

- **تسجيل سنتر/منفرد**: نوع الحساب + البيانات + فحص الكود/التوفر + `signUp` ← جلسة؟ فوراً (`complete_center_registration` + تجريبي trial 14 يوماً) : معلق + تأكيد + دخول فيُستكمل ← الحارس → `/dashboard`.
- **تسجيل طالب**: كود/باركود (موقوف=مرفوض) ← بطاقة السنتر ← صف/مجموعة (قوائم آمنة) ← توفر ← `signUp` ← فوراً/معلق ← ربط بسجل هاتف موجود إن وُجد ← `/home`. الكود لا يُطلب بعدها أبداً.
- **تسجيل فريق**: كود + صفة (مدرس/مدير/سكرتير) ← حساب **خامل** ← المالك يفعّل + صلاحيات + مجموعات (بحدود الباقة خادمياً).
- **الدخول**: بريد+كلمة ← فحص الدور الفعلي + `is_active` + مطابقة النوع مع الشاشة (للمعلق خصوصاً) ← استكمال المعلق (مع الاحتفاظ به عند الفشل) ← الحارس (اشتراك/إيقاف/موقوف) ← التوجيه. الموقوف إدارياً يُطرد فوراً.
- **طلب ترقية**: المالك (صفحة الباقات) ← خطة+مدة+تاريخ وقيمة التحويل ← `pending` ← المطور يعتمد فيُنشأ الاشتراك + سجل ← أو يرفض. السحب متاح للمعلق.
- **تغيير القاعدة/إصدار تحديث/RouterGuard**: كما كانت (استبعاد `(` من المسارات؛ المسارات الفعلية محدثة في `_layout` و`audit-app`).

---

## 9) الثوابت التي يُمنع كسرها

يحميها `npm test` آلياً — قبل أي تعديل تحقق:
1. لا RLS واسعة `USING (true)` أبداً.
2. العزل خادمي: أي كيان متعدد السناتر = `center_id` + سياسات + تسجيل في `audit-sql`.
3. كود السنتر ثابت وفريد؛ status للمطور (مشغّلات).
4. الطالب قراءة نفسه فقط عبر `my_student_id()`؛ الامتحانات والمحاولات المباشرة والإشعارات عبر RPC فقط.
5. القائمة البيضاء `legacy_admins` فقط — ممنوع «بلا ملف = أدمن».
6. البريد/الهاتف فريدان عالمياً + موفرين معروفين + ولي مختلف (عميل وخادم).
7. أسماء الموقع المشتركة لا تتغير نوعها؛ زيادات بأعمدة فقط.
8. لا تُعِد تشغيل مخطط الموقع القديم بعد مخططنا أبداً.
9. `extra.configUrl` ثابت وحيد — تغييره = إصدار جديد.
10. RTL والعربية + تباين نصوص التدرجات الداكنة (`#052E22` على النعناعي).
11. الإصدار: `app.json` + العامل معاً (فحص توافق آلي).
12. `npm test` + `typecheck` + `expo export` قبل الرفع.
13. أي RPC تُغيَّر وسائطه/إرجاعه: `DROP FUNCTION` أولاً + تعريف الدوال قبل سياساتها + فحص `audit-rpc` أخضر.
14. المنع الافتراضي للفريق: أي شاشة/زر جديد يُفحص بـ `can()` أو `isOwner` + يُسجل في `TEACHER_SCREENS`/التبويبات + فحص `audit-app`.
15. أرقام الباقات والحدود في `billing.ts` فقط — يغطيها `test-utils` (أي تغيير سعر يكسر الفحص عمداً للمراجعة).
16. نطاق المدرس: students/groups/schedule (وكل شاشة بيانات) تقصر قوائمها على `useTeacherGroupIds` — المدرس لا يرى إلا مجموعاته المسندة وطلابها. ملف الطالب: المستحقات والدفعات بصلاحية collect/reports فقط. guide/schedule عرضيتان (قراءة فقط) بلا بوابة صلاحية.
16. لا أسرار في الكود/الشات (`service_role` للعامل Secrets والسكريبتات فقط، ويُبدَّل بعد المشاركة).

---

## 10) الاختبارات والتحقق

| الجناح | الفحوص | الوصف |
|---|---|---|
| `test-fresh.mjs` | 168 | منظومة مستقلة كلياً: باقات حرفية · مصفوفة صلاحيات كل دور · بوابات كل شاشة · نطاق المدرس · ثيم حي بتباين WCAG · تجميع بث المطور · rate limit بكل الصيغ · بنية تنقل · عزل api · مخطط القاعدة |
| `test-utils.mjs` | 95 | منطق نقي (utils+billing بتجميع tsc): تطبيع/صيغ/وقت/تعارض/بريد/روابط/امتحانات/باقات وأسعار |
| `test-qr.mjs` | 15 | ترميز/فك الطالب والسنتر (يشمل عربياً) + رفض الدخيل/المعبث + اليومية |
| `test-worker.mjs` | 22 | مسارات العامل + CORS + توافق الإصدار + `/push/notify` (403/400) |
| `audit-sql.mjs` | 117 | بنية/RLS/عزل/قائمة بيضاء/مشغلات/فرادة/RPC/جداول جديدة/ترتيب التعريف/DROPs |
| `audit-rpc.mjs` | 30 | تطابق كل `rpc()` في التطبيق مع تعريفات المخطط وسيطاً وسيطاً (يكشف قاعدة منشورة أقدم) |
| `audit-app.mjs` | 113 | إعدادات/تبعيات/بوابات/خرائط المدرس/شاشات/حارس/تحديث/كاميرا/واتساب/بث |

+ `typecheck` صارم + `expo export --platform android`. **الأخضر الكلي الحالي: 392.**
+ **الحي** `npm run test:e2e` (منفصل): زائر/مالك/طالبان/سنتر ثان/مدرس/مطور/بوابة/طلبات — يتطلب `E2E_SUPABASE_URL/ANON_KEY/SERVICE_KEY` + تشغيل المخطط أولاً، وينظف وراءه، و**بدّل service_role بعده**.

---

## 11) البناء والتوزيع والتحديثات

- `npm install && npx expo start` (عند كاش قديم: `-c`) ← Expo Go. **الثيم/الكود الجديد لا يظهر في APK مثبت قديم إطلاقاً — يلزم بناء جديد.**
- APK: `eas build --platform android --profile preview` · AAB: `--profile production` (نفس الشهادة شرط التحديث-فوق-القديم).
- التحديث الذاتي عبر R2+العامل. iOS مستقبلاً عبر المتجر.
- الدفع الفوري: مشروع EAS (`eas init` لمعرف المشروع) + Secrets العامل + Cron — بدونه الإشعارات داخل التطبيق فقط.

---

## 12) علاقة التطبيق بموقع الويب القديم

كما كانت: نفس الجداول + `center_id`، سياسات `legacy` للقائمة البيضاء، anon يقرأ NULL فقط، الترقية لاحقاً بملء `center_id` واعتماد Auth. إضافة قديم للقائمة:
`UPDATE public.app_config SET value = value || '"<UID>"'::jsonb WHERE key='legacy_admins';`

---

## 13) بيئة العمل وخبرات عملية مهمة

- **Node 24 / npm**؛ سجل npm يعمل؛ **expo API محجوبة** — ثبّت يدوياً بنسخ SDK 52 المتوافقة: `expo-camera@^16` · `react-native-svg@^15` · `react-native-qrcode-svg@^6` · `expo-print@^14` · `expo-sharing@^13` · `expo-clipboard@^7` · `expo-notifications@~0.29.14` (نسخ 0.x لجيل 52 — لا توجد 3/4!). تحقق من `npm view` عند الشك.
- لا Android SDK/Java — التحقق `tsc` + `expo export` + ساكن؛ APK عبر EAS عند المستخدم.
- `node_modules` لا تُحفظ بين الجلسات — ابدأ بـ `npm install`. **لا تنفذ `npm audit fix`** — يكسر تثبيتات Expo.
- PowerShell 5.1: بلا `&&` (استخدم `;`)، وانتبه لترميز الكتابة (فضّل أدوات التحرير على Set-Content).
- فرع العمل **`arena/01a08697-mr-center-android`** — ادفع إليه فقط. لا تضع أسراراً في الريبو (`.npmrc` و`.wrangler/` و`CODELY.*` غير مُتتبعة — أبقها كذلك).
- Metro: أي `SyntaxError` عابر بعد تعديل = كاش قديم غالباً (`-c`)؛ تحقق أولاً من توازن الأقواس.
- `getExpoPushTokenAsync` يحتاج `extra.eas.projectId` — بدونه يتخطى صامتاً.

---

## 14) قيود معروفة وخطط مستقبلية

- الدمج بالهاتف: من يعرف هاتف طالب + الكود قد يرتبط بسجله — المسئول يرى البريد المرتبط ويصحح.
- التسعير الأسبوعي = السعر ×4 ثابت؛ المتوسطات غير مرجحة زمنياً؛ `check_availability` كاشفة جزئياً (مقصودة + حدود معدل).
- الإتاحة الزمنية: `formatDate` بتوقيت الجهاز (مصر آمنة)؛ تذكير العامل بتوقيت UTC+إزاحة (راجع DST صيفاً/شتاءً).
- نسخة APK المثبتة لا تتحدث ذاتياً قبل إعداد R2+UPDATE.
- حسابات الفريق المحذوفة تبقي حساب Auth يتيماً (بلا ملف → «غير مكتمل» عند الدخول) — يُحذف من لوحة Auth عند اللزوم.
- ترقية الموقع لاحقاً + iOS عبر المتجر + Webhook فوري للبث (موثق، يدوي الإعداد).

---

## 15) قاموس مصطلحات المالك

| يقصد | المعنى عندنا |
|---|---|
| «السنتر» | المركز المتكامل (`kind='center'`) — بفريق تابع |
| «مدرس خصوصي/منفرد» | (`kind='solo'`) — نفس الوظائف بلا فريق، حتى 200 طالب |
| «كود السنتر» | `code` 3–8 ثابت وفريد + باركود MRC0 للطباعة |
| «فريق العمل/المدرس» | `teacher/manager/secretary` — خامل حتى التفعيل + 10 صلاحيات + مجموعات مسندة |
| «المدير/السكرتير» | أدوار فريق بحدود الباقة (1/1-2/2-4) تُفرض خادمياً |
| «الباقة/الترقية» | trial(14 يوماً) → center_full / center_medium / solo_teacher — طلب بتحويل يعتمده المطور |
| «سجل العمليات/المعاملات» | `activity_log` / `subscription_requests` |
| «الإشعار/البث» | داخلي مجاني (صف/رسالة + مقروءية) + فوري عبر العامل (push/cron) |
| «سوبر أدمن / المطور» | `super_admin` — المالك نفسه |
| «مفاتيح الربط» | url + anon_key |
| «كلاود فلير» | العامل (`worker.js` + R2 + Secrets + Cron) |
| «تحديث التطبيق» | فحص الإصدار ← تحميل APK ← تثبيت فوق القديم |

---

## 16) قائمة فحص قبل التسليم

- [ ] `npm test` — الأجنحة السبعة خضراء (وأضِف فحوصاً لما أضفته).
- [ ] `npm run typecheck` — صفر أخطاء.
- [ ] `CI=1 npx expo export --platform android` — الحزمة تُبنى.
- [ ] القسم 9 بنداً بنداً + العربية RTL + رسائل معربة بزر نسخ.
- [ ] أي شاشة/دور/جدول/RPC جديد مسجل في (الحارس + التبويبات + audit-app/audit-sql/audit-rpc).
- [ ] AGENTS.md + README محدثان لو تغيّر سلوك أو إعداد.
- [ ] كوميت عربي واضح + دفع لفرع العمل نفسه + بلا أسرار.
