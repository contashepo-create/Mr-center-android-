# 🤖 AGENTS.md — الملف التوجيهي الشامل لمنصة Mr Center

> **هذه الوثيقة مكتوبة لوكيل الذكاء الاصطناعي الذي سيعمل على هذا المشروع.**
> اقرأها كاملة قبل لمس أي سطر. هي المرجع النهائي لفهم المشروع من أوله لآخره:
> ماذا يريد المالك، كيف بُني، لماذا اتُّخذت كل قرار، وما الذي يُمنع كسره.
>
> آخر تحديث: سبتمبر 2026 — الحالة: المشروع يمر بفحوصات الكود الحالية، لكن الترحيل إلى Supabase الفعلي واختبار جهاز Android حقيقي لم يُنفذا داخل هذه الجلسة.

---

## جدول المحتويات

1. [الهوية والرؤية](#1-الهوية-والرؤية)
2. [ما يريده المالك حرفياً (متطلبات مُلزِمة)](#2-ما-يريده-المالك-حرفيا-متطلبات-مُلزِمة)
3. [الخريطة المعمارية الكاملة](#3-الخريطة-المعمارية-الكاملة)
4. [نموذج تعدد السناتر والصلاحيات](#4-نموذج-تعدد-السناتر-والصلاحيات)
5. [قاعدة البيانات بالتفصيل](#5-قاعدة-البيانات-بالتفصيل)
6. [نظام كلاود فلير (الوسيط المركزي)](#6-نظام-كلاود-فلير-الوسيط-المركزي)
7. [بنية كود التطبيق ملفاً ملفاً](#7-بنية-كود-التطبيق-ملفاً-ملفاً)
8. [تدفقات الاستخدام الحرجة](#8-تدفقات-الاستخدام-الحرجة)
9. [الثوابت التي يُمنع كسرها](#9-الثوابت-التي-يُمنع-كسرها)
10. [الاختبارات والتحقق](#10-الاختبارات-والتحقق)
11. [البناء والتوزيع والتحديثات](#11-البناء-والتوزيع-والتحديثات)
12. [منصة الويب — الوجه الثاني للمنتج](#12-منصة-الويب--الوجه-الثاني-للمنتج)
13. [بيئة العمل وخبرات عملية مهمة](#13-بيئة-العمل-وخبرات-عملية-مهمة)
14. [قيود معروفة وخطط مستقبلية](#14-قيود-معروفة-وخطط-مستقبلية)
15. [قاموس مصطلحات المالك](#15-قاموس-مصطلحات-المالك)
16. [قائمة فحص قبل التسليم](#16-قائمة-فحص-قبل-التسليم)

---

## 1) الهوية والرؤية

- **المنتج**: منصة متكاملة لإدارة «السناتر» التعليمية المصرية — تعمل على **ويب وموبايل معاً** كمنتج واحد بوجهين. الطلاب (متعددو المجموعات)، مجموعات بمدرسين وتسعير متنوع، حضور (يدوي + QR)، مدفوعات ومستحقات، درجات يدوية والكترونية، اختبارات بأنواع، تقارير PDF، واتساب، إشعارات داخلية وفورية، استبيانات، طلبات، مكتبة (شرف/ملفات/روابط)، جدول أسبوعي، فريق عمل بصلاحيات، باقات واشتراكات، محاسبة.
- **التقنية**: واجهتان ندّيتان تشتركان في نفس كل شيء:
  - **موبايل**: React Native + Expo SDK 52 + TypeScript + expo-router v4 — يعمل على أندرويد الآن، وعلى iOS لاحقاً بنفس الكود.
  - **ويب**: Next.js 16 + TypeScript + React 19 — يعمل على كل المتصفحات والأجهزة.
- **القاعدة واحدة**: Supabase (PostgreSQL + Auth + RLS) — **نفس مشروع Supabase بالضبط** للويب والموبايل. أي بيانات تُنشأ أو تُعدّل من أي وجه تظهر في الوجه الآخر فوراً لأنهما على نفس الجداول ونفس دوال RPC ونفس سياسات RLS.
- **اللغة**: واجهة عربية 100% باتجاه RTL مفروض على الوجهين (`I18nManager.forceRTL` + `direction:'rtl'` على الموبايل، `<html lang="ar" dir="rtl">` على الويب). تعليقات الكود والتوثيق بالعربية. المالك يتكلم عربية (لهجة مصرية) — خاطبه بها.
- **الهوية البصرية**: «كربون المستقبل» — داكن فحمي + نعناعي كهربائي `#00E5A0` + كهرماني، مع أنيميشن ضغط (spring) على الأزرار والبطاقات. يدعم الوضع الداكن والفاتح فورياً على الوجهين عبر ThemeProvider وAsyncStorage/themedStyles (موبايل) و CSS variables + localStorage (ويب)؛ لا تفترض أن الوضع داكن فقط.
- **الوسيط**: عامل Cloudflare (Worker) واحد هو نقطة التحكم المركزية لمفاتيح القاعدة وللتحديثات وللدفع الفوري — يقرأه الويب والموبايل معاً (راجع القسم 6).

### المستودعات المرتبطة
| المستودع | الدور | الوجه |
|---|---|---|
| [`contashepo-create/Mr-center-android-`](https://github.com/contashepo-create/Mr-center-android-.git) (هذا) | تطبيق الجوال + مخطط SQL + عامل كلاود فلير + الأدلة | 📱 موبايل |
| [`contashepo-create/mr-center-web`](https://github.com/contashepo-create/mr-center-web.git) | موقع الويب — منصة ندّية تماماً تشترك في نفس كل شيء | 🌐 ويب |

**مبدأ حاسم**: المستودعان وجهان لنفس المنتج ونفس البيانات. لا يوجد «قديم» و«جديد» — كلاهما يخدم نفس السناتر ونفس المستخدمين ونفس الجداول. التغيير في المخطط أو الصلاحيات أو البيانات يجب أن يُنشر في المستودعين معاً ويُختبر على الوجهين.

> 📌 **ملاحظة لأي وكيل يعمل مستقبلاً على أحد المستودعين**: إذا وصلتك تعليمات أو Diff يخص
> «تحديث الويب والموبايل معاً»، فهذا يعني أن نفس التغيير المنطقي (خاصة ملفات `supabase/*.sql`
> الجديدة) يجب أن يظهر في المستودعين. ملفات الترحيل (migrations) في `supabase/` بهذا المستودع
> هي **نفسها بالحرف** الموجودة في `supabase/` بمستودع الويب (نفس الاسم بنفس التاريخ)، لأن الاثنين
> يشغّلانها على **نفس مشروع Supabase**. عند الحاجة لنسخ ترحيل ناقص من هذا المستودع، أسرع طريقة
> موثوقة هي استنساخ مستودع الويب (`git clone https://github.com/contashepo-create/mr-center-web.git`)
> ونسخ ملف `supabase/<same-name>.sql` منه بدل إعادة كتابته من الذاكرة — لتفادي أي فرق دقيق في
> منطق RLS/RPC الحساس. أما كود التطبيق نفسه (شاشات Expo مقابل صفحات Next.js) فمنفصل تماماً في كل
> مستودع ولا يُنسخ حرفياً؛ فقط منطق البيانات والمخطط مشترك حرفياً (راجع القسم 12 لتفاصيل الفروق
> المقبولة في طبقة العرض).

- فرع العمل في هذا المستودع: **`arena/01a08697-mr-center-android`** — اعمل عليه فقط وادفع إليه فقط.

---

## 2) ما يريده المالك حرفياً (متطلبات مُلزِمة)

هذه ليست اقتراحات — هي مواصفة المنتج. أي تغيير مستقبلي يجب أن يحافظ عليها:

1. **منصة ويب + موبايل كاملة احترافية** تعمل على كل الأجهزة وأحجام الشاشات، بهوية مستقبلية وأنيميشن سلس، وشاشات لكل قسم — بنفس البيانات والصلاحيات على الوجهين.
2. **الربط بنفس قاعدة Supabase** — الويب والموبايل يقرآن ويكتبان على نفس الجداول. أي عملية من أي وجه تظهر في الآخر فوراً. حتى لو أُغلق أحد الوجهين، يعمل الآخر مستقلاً بالكامل.
3. **نظام صلاحيات من أربع فئات** (متطابق على الويب والموبايل):
   - **سوبر أدمن = المطور = المالك**. الوحيد الذي تظهر له لوحة تحكم المطور.
   - **صاحب السنتر** (`center_admin`): صلاحيات كاملة داخل سنتره فقط.
   - **فريق العمل** (`teacher`/`manager`/`secretary`): يسجلون بكود السنتر، وحساباتهم **خاملة حتى يفعّلها المالك**، ويعملون داخل **صلاحيات مفصلة (10 مفاتيح)** ومجموعات مسندة فقط. **المنع هو الافتراضي** — كل ما لم يُمنح صراحة محجوب.
   - **طالب**: يرى بياناته فقط (حضوره، درجاته، مدفوعاته، إعلانات وإشعارات سنتره).
4. **عزل كامل بين المستخدمين بعمود `center_id`** — سنتر لا يرى بيانات سنتر آخر إطلاقاً، والطالب لا يرى إلا سجلاته هو (مثبت باختبار حي E2E على القاعدة الحقيقية) — على الويب والموبايل معاً.
5. **كود السنتر**: يختاره صاحبه عند التسجيل من 3–8 حروف أو أرقام (إنجليزي/عربي/مختلط)، **ثابت لا يتغير** و**فريد على مستوى النظام**؛ رسالة «الكود غير متاح» إن كان مأخوذاً، وتنبيه مسبق أن الكود ثابت وفريد. + **باركود سنتر ثابت** للطباعة على المكتب (يحمل الكود والاسم) يُمسح بدل الكتابة — على الويب والموبايل.
6. **تسجيل الطالب بكود السنتر مرة واحدة فقط** — يرى اسم سنتره للتأكيد، ويختار **صفه ومجموعته من قوائم**، ويُربط بـ `center_id` تلقائياً؛ **عند الدخول لاحقاً لا يُطلب الكود أبداً**. الطالب قد ينضم **لأكثر من مجموعة** (مواد/مدرسون مختلفون) — من التسجيل أو من ملفه لاحقاً.
7. **الحسابات فريدة عالمياً**: البريد ورقم الهاتف لا يتكرران في النظام كله — «الإيميل مستخدم» / «الرقم مستخدم». البريد من **موفرين معروفين فقط** (Gmail/Yahoo/Outlook/...) — المؤقتة مرفوضة. **رقم ولي الأمر إجباري ومختلف عن رقم الطالب** (واجهة + خادم) — على الويب والموبايل.
8. **نوعا الحسابات**: **سنتر متكامل** (صلاحيات كاملة + فريق تابع — اشتراكه أعلى) و**مدرس خصوصي مستقل** (`solo`: نفس الوظائف لمواده وطلابه بكوده، بلا فريق، حتى 200 طالب) — على الويب والموبايل.
9. **الصفحة الرئيسية**: دخول كبير (مسئول/طالب) + تسجيل صغير (سنتر/طالب) + دخول/انضمام فريق العمل + حول التطبيق — على الويب والموبايل.
10. **لوحة تحكم المطور مخفية تماماً** — ضغطة مطوّلة (٥ ثوانٍ) على الشعار أو ٥ نقرات فوتر (موبايل) / بوابة بريد محمية (ويب) + دخول حساب المطور.
11. **من لوحة المطور**: مفاتيح القاعدة لكل العملاء · باقات احترافية واعتماد طلبات الترقية · إيقاف/تفعيل أي سنتر · ملف أي سنتر كامل (مالك/فريق/طلاب/اشتراكات/نشاط) · بث للجميع/الأصحاب/سنتر مع تتبع القراءة · محرر «حول التطبيق» والرسالة العامة.
12. **الباقات**: تجريبية ١٤ يوماً بمزايا كاملة → شاملة (600/6500/12000: 0 مدير مضاف+2 سكرتير+4 مدرسين) / متوسطة (400/4500/8500: 0+1+2) / خصوصي (300/3000/5000: حتى 200 طالب). الطلب من صفحة الباقات (خطة+مدة+تاريخ وقيمة التحويل) ويعتمده المطور. الحدود **مفروضة خادمياً** (مشغّل + عدّ) — على الويب والموبايل.
13. **مفاتيح قاعدة البيانات عبر كلاود فلير كوسيط واحد** + **التحديث الذاتي** (للموبايل) + **الدفع الفوري والتذكير** عبر العامل (راجع القسم 6).

---

## 3) الخريطة المعمارية الكاملة

```
┌──────────────────────────┐     ┌──────────────────────────┐
│      تطبيق الجوال        │     │      منصة الويب          │
│  (Expo / React Native)   │     │  (Next.js / React)       │
│  📱 Android الآن · iOS لاحقاً  │     │  🌐 كل المتصفحات         │
└───────────┬──────────────┘     └───────────┬──────────────┘
            │                                │
            │  ① عند كل إقلاع:               │  عند كل تحميل:
            │  GET <configUrl>               │  GET <configUrl>
            ▼                                ▼
┌──────────────────────────────────────────────────────────┐
│              عامل Cloudflare Worker واحد                 │
│  /config: database+update  ·  /push/notify + cron: دفع   │
│  نفس العامل الذي يقرأه الويب والموبايل معاً              │
└───────────┬──────────────────────────────────────────────┘
            │ ② عميل Supabase
            ▼
            ┌──────────────────────────────────────────────┐
            │   Supabase واحد (PostgreSQL + Auth + RLS)    │
            │   نفس المشروع = نفس الجداول = بيانات متطابقة │
            │   ويب ⇄ موبايل = نفس المستخدمين والصلاحيات │
            │   العزل والأمان على الخادم عبر RLS           │
            └──────────────────────────────────────────────┘
```

**مبدأ حاسم**: التطبيق والويب لا يخزنان أسراراً. المفاتيح من كلاود فلير، والأمان كله RLS — فمفتاح anon الظاهر لا يمنح شيئاً تمنعه السياسات. مفتاح `service_role` لا يظهر في التطبيق ولا في الويب إطلاقاً (للعامل وسكريبتات الإدارة فقط، ويُبدَّل بعد أي مشاركة).

**التطابق التام**: كل جدول، كل عمود، كل سياسة RLS، كل دالة RPC، كل مشغّل في المخطط يخدم الويب والموبايل معاً. لا يوجد مسار كود يخص وجهًا واحدًا دون الآخر في منطق البيانات — الاختلاف فقط في طبقة العرض (UI).

---

## 4) نموذج تعدد السناتر والصلاحيات

### الجداول الجديدة (أنشأها مخططنا)
| الجدول | الغرض | أعمدة مفتاحية |
|---|---|---|
| `centers` | السناتر | `id` uuid · `name` · `code` (فريد بـ upper، ثابت) · `kind` ('center'/'solo') · `owner_*` · `status` |
| `profiles` | كل مستخدم Auth ودوره | `id`=auth.uid · `role` (6 أدوار) · `center_id` · `student_id` · `is_active` (الفريق خامل حتى التفعيل) · `perms` (10 صلاحيات jsonb) · `push_token` |
| `student_groups` | عضويات الطالب الإضافية | `PRIMARY KEY(student,group)` + عدّاد المجموعة يشمل الكل |
| `teacher_groups` | إسناد المجموعات للفريق (نطاق عمله) | `PRIMARY KEY(teacher,group)` |
| `center_subscriptions` | الاشتراكات | `plan_type` (trial/monthly/yearly/custom/center_full/center_medium/solo_teacher) · `starts_on/ends_on` · `status` |
| `subscription_requests` | طلبات الترقية | `plan/months/amount/transfer_at` · `status` (pending/approved/rejected) — كتابة المالك pending فقط |
| `activity_log` | سجل عمليات لا يُحذف | كتابة الفريق المفعّل، قراءة المالك والمطور |
| `app_exams`/`app_exam_attempts` | اختبارات (8 أنواع بدرجات) + محاولة واحدة (التصحيح عبر RPC) | `questions/answers` jsonb · الأنواع: mcq/multi/tf/complete/match تلقائي + correct/essay/short يدوي · `status` (graded/pending_review) · `UNIQUE(exam,student)` — **بلا إدخال مباشر للطالب عمداً** |
| `support_messages` | قناة الدعم (مالك ↔ مطور) | `sender_role` (owner/developer) · سياسات: owner يقرأ ويرسل لسنتره والمطور الكل (`support_owner_read/insert` + `support_super_admin`) |
| `app_inquiries` | طلبات (سؤال/نقل/تسجيل) + رد وحالة | `kind` · `status` · `reply` |
| `app_surveys`/`app_survey_responses` | استبيانات + رد واحد | `is_active` · `UNIQUE(survey,student)` |
| `center_settings` | إعدادات تشغيلية | `settings` jsonb (واتساب/فتح التسجيل/سنة الأرشيف) |
| `app_notifications`/`app_notification_reads` | بث داخلي (صف/رسالة) + مقروءية | `audience` (all/grade/group/student/**owners**) · `UNIQUE(notification,student)` — الطالب عبر RPC مفلتر فقط |
| `app_config` | عامة | `'public_config'` (قراءة عامة، كتابة مطور) · `'legacy_admins'` (القائمة البيضاء) |

### جداول الموقع القديم الموسَّعة
`grades` · `groups` (+`teacher_name/phone` + تسعير `billing_type/weekly_price/session_price`) · `students` (+`guardian_phone`) · `dues` · `payments` · `sessions` · `attendance` · `announcements` · `manual_grades` · `exams` · `honorees` (+`student_id/group_id`) · `shared_files` · `important_links` — كلها `center_id` (القديم NULL ويخص الموقع فقط).

### قواعد العزل (RLS)
- **دوال** `my_role/my_center_id/my_student_id/is_legacy_admin/center_is_active/admin_owns_center/teacher_is_active/teacher_center_ok` (`SECURITY DEFINER`).
- **الطالب**: قراءة نفسه فقط (`my_student_id`) + سنتره العام (`my_center_id`) + عضوياته + RPC الامتحانات/الإشعارات. لا كتابة إطلاقاً.
- **الفريق المفعّل** (`teacher/manager/secretary`): قراءة سنتره + كتابة (حضور/درجات/اختبارات/إعلانات/استبيانات/شرف/طلبات-رد/إشعارات/تحصيل) — والواجهة تحجب فوق ذلك بالصلاحيات العشر (`can()` + `NoAccess` + تبويبات مخفية + منع افتراضي للقوائم).
- **المسئول**: كامل على سنتره الفعّال + إدارة فريقه (تفعيل/صلاحيات/حذف عبر `profiles_teacher_manage/_delete` والمشغّل).
- **المطور/النظام القديم**: كامل. **الزائر**: `public_config` + صفوف NULL القديمة + `lookup/check_availability/signup_lists`.

### دوال RPC
| الدالة | الأذونات | الوظيفة |
|---|---|---|
| `get_published_exams()` | auth | امتحانات السنتر بلا إجابات + هل حاولها |
| `submit_exam_attempt(exam,answers)` | auth | تصحيح (mcq/tf بدرجات، مقالي→مراجعة) + منع التكرار |
| `lookup_center_by_code(text)` | anon+auth | `{id,name,owner_name,status}` — يميز الموقوف (DROP قبل تغيير الشكل) |
| `check_registration_availability(email,phone)` | anon+auth | `{email_taken, phone_taken}` |
| `get_center_signup_lists(uuid)` | anon+auth | أسماء الصفوف/المجموعات للسنتر الفعّال فقط (للتسجيل) |
| `complete_center_registration(5 args)` | auth | سنتر+ملف+تجريبي 14 يوماً (`trial`) + نوع الحساب |
| `complete_student_registration(6 args)` | auth | بوابة التسجيل + ربط بسجل موجود + صف/مجموعة + سقف 200 للمنفرد + رقم ولي مختلف |
| `register_staff_account(center,name,phone,role)` | auth | حساب فريق خامل (يرفض المنفرد `staff_not_allowed`) |
| `get_my_notifications()` | auth | إشعارات الطالب مفلترة + مقروءية |
| `get_my_subscription()` | auth | `{status, plan_type, ends_on, days_left, center_status}` (سوبر → active) |

### المشغلات والقيود
- `trg_guard_center_code`: الكود وstatus للمطور فقط. `trg_guard_profile_identity`: الهوية للمطور + استثناء إدارة المسئول لفريقه. `trg_sync_group_count(+junction)`: عدّاد المجموعة (أساسية+إضافية). `trg_staff_limit_check`: حدود الباقة خادمياً (`staff_limit_reached`).
- فرادة: `centers_code_unique` · `profiles_email_unique` · `profiles_phone_unique` · `UNIQUE(exam,student)` · `UNIQUE(survey,student)` · `UNIQUE(notification,student)`.
- **تحذير النسخ**: أي دالة يُغيَّر توقيعها/نوع إرجاعها تُسقط بـ `DROP FUNCTION` أولاً (خطأ 42P13)، والدوال قبل السياسات التي تستخدمها (خطأ 42883) — ويفحصهما `audit-sql` آلياً.

---

## 5) قاعدة البيانات بالتفصيل

ملف المخطط الوحيد: **`supabase/android_multitenant_schema.sql`** — آمن لإعادة التشغيل (IF NOT EXISTS / OR REPLACE / DROP IF EXISTS) داخل معاملة واحدة، لا يحذف بيانات ولا يكسر الموقع. يبذر `public_config` و`legacy_admins` من `auth.users` لحظة الترحيل. **لا تُعِد تشغيل مخطط الموقع القديم بعده أبداً.**

إعدادات Auth: «Allow new users to sign up» + **«Confirm email» مفعّل** (لمنع الوهمي). التسجيل على مرحلتين: `signUp` ← معلق (`pendingRegistration.ts`: سنتر/طالب/فريق) ← تأكيد البريد ← أول دخول يستكمل تلقائياً (`LoginForm` + مطابقة النوع مع الشاشة + الاحتفاظ بالمعلق عند الفشل). البريد من موفرين معروفين فقط (فحص عميلي بقائمة) ورقم الولي إجباري ومختلف (عميل + خادم `same_guardian_phone`).

صلاحيات الجداول: `GRANT ... TO anon, authenticated, service_role` (الأخير لعمليات الخادم فقط — العامل والسكريبتات — ويُبدَّل بعد أي مشاركة).

إنشاء المطور:
```sql
INSERT INTO public.profiles (id, role, full_name, email)
VALUES ('<UID>', 'super_admin', 'المطور', 'dev@mail.com');
```

---

## 6) نظام كلاود فلير (الوسيط المركزي)

### العامل — `cloudflare/worker.js`
يعدّله المالك من اللوحة ثم Deploy: `DATABASE` (المفاتيح) · `UPDATE` (الإصدار/APK/الإجبار). المسارات: `/config` (و`/`) · `/version` · `/database` · `/push/notify` (POST + `x-push-secret`) · CORS مفتوح + كاش 60 ثانية.

### الدفع الفوري والتذكير (اختياري — مجاني)
- متغيرات Secrets مشفرة: `SUPABASE_URL` + `SUPABASE_SERVICE_KEY` + `PUSH_SECRET` + `TZ_OFFSET_MINUTES` (مصر 120/180).
- Cron كل 15 دقيقة → تذكير الحصص (مجموعات تبدأ خلال ~ساعة، تشمل العضويات الإضافية) حتى والتطبيق مغلق.
- Webhook عند INSERT في `app_notifications` → `/push/notify` (الدليل في `cloudflare/README.md`).
- رمز الدفع يُسجل صامتاً عند الدخول (`push_token`) — يتطلب معرف مشروع EAS في نسخة المتجر.

### التهيئة والتحديث
سلسلة `initSupabase()`: يدوي → كلاود فلير (مع كاش آخر رد) → مخزن → مدمج → شاشة `ConnectionSetup`. الثابت الوحيد `extra.configUrl`. التحديث الذاتي عبر R2 + `UPDATE` (`REQUEST_INSTALL_PACKAGES`)، يتخطى DEV/Expo Go/غير أندرويد.

---

## 7) بنية كود التطبيق ملفاً ملفاً

```
index.ts                  فرض RTL ثم expo-router/entry
app.json                  com.mrcenter.app · CAMERA + plugins (camera recordAudio=false) · extra.configUrl
eas.json                  preview=APK · production=AAB (autoIncrement)

app/  (مجلدات (x) بلا مسار)
├─ _layout.tsx            الجذر: SessionProvider + RouterGuard (أدوار/اشتراك/إيقاف/موقوف) + <UpdateManager/> (تلاشي fade بخلفية داكنة)
├─ index.tsx              الترحيب: دخول كبير + تسجيل صغير + فريق العمل + حول + بوابة المطور (5 ثوانٍ/5 نقرات) + ConnectionSetup
├─ about.tsx              حول: محتوى المطور + قائمة كل المميزات + تواصل
├─ blocked.tsx            حظر الاشتراك (حالة/باقة/تواريخ معربة + واتساب + إعادة فحص)
├─ auth/                  login-admin/student/teacher (أخيرة للفريق) · register-center (نوع الحساب center/solo + ملاحظة الأسعار) · register-student (كود/باركود + صف/مجموعة) · register-teacher (فريق: مدرس/مدير/سكرتير)
├─ developer/             بوابة لكل صفحة (DeveloperGate) + index (إحصائيات تشمل الفريق) + connection (خروج إجباري عند تبديل القاعدة) + broadcast (تجميع البثوث + معاينة + تتبع) + centers (واتساب المالك + تفاصيل) + center-detail (ملف السنتر الكامل) + subscriptions (منتجات + اعتماد طلبات) + app-info
├─ (admin)/               تبويبات (إخفاء حسب صلاحيات الفريق + شارة اشتراك للمالك فقط)
│  ├─ dashboard.tsx       ترحيب + (اشتراك للمالك فقط) + إحصائيات + إجراءات مفلترة بالصلاحية
│  ├─ students.tsx        بحث معقم + فلاتر حالة/مجموعة بعدادات + ترتيب أبجدي دائم + عداد «عرض X من Y» + بطاقة بحرف الاسم + (إدارة للمالك فقط)
│  ├─ groups.tsx          CRUD (للمالك) + TimePicker + تسعير (شهري/أسبوعي/بالحصة) + مدرس + بطاقات غنية + تحقق أيام/زمن + تنبيه تعارض
│  ├─ attendance.tsx      تنقل تاريخ بسقف اليوم + كشف بلا حصص وهمية (تُنشأ عند الحفظ) + حاضر/متأخر/غائب + كل حاضر/غائب + ماسح
│  ├─ scan.tsx            كاميرا بلا أطفال (طبقة عائمة) + إذن تلقائي + إعدادات عند الرفض + بحث يدوي + رفض غير النشط + تحصيل بالأقدم
│  ├─ teachers.tsx        الفريق: فلتر أدوار + حدود الباقة + تفعيل/إيقاف/حذف + 10 صلاحيات + إسناد مجموعات
│  ├─ subscription.tsx    الباقات (للمالك فقط): خطته + المنتجات + طلب ترقية (خطة/مدة/تاريخ وقيمة التحويل) + سحب الطلب
│  ├─ activity.tsx        سجل العمليات (للمالك فقط)
│  ├─ payments.tsx        توليد حسب التسعير (طالب+مجموعة) + معلق/جزئي(Dفع ناقص)/مسدد + سجلات الأيتام بلا تنقل
│  ├─ exams.tsx           8 أنواع (اختياري/متعدد/صح-خطأ/أكمل/وصل/صحّح/مقالي/قصير): 5 قوالب + معاينة + منع نشر ناقص + تحذير تعديل المنشور + نتائج + تصحيح يدوي + PDF
│  ├─ inquiries.tsx       فلتر + رد (رد/قبول/رفض) + إغلاق + فتح ملف الطالب للنقل
│  ├─ surveys.tsx         إنشاء + تفعيل + نتائج + قفل الأسئلة المُجابة (فحص لحظي)
│  ├─ library.tsx         شرف (اختيار طالب مفلتر + اسم يدوي للأرشيف) + ملفات + روابط (تحقق URL)
│  ├─ schedule.tsx        أسبوعي + تعارض + مجموعات بلا أيام + أسعار حسب التسعير + PDF
│  ├─ reports.tsx         4 تبويبات: نظرة عامة (حضور/تحصيل/متوسطات) + تقرير طالب (شامل/شهري/مالي/أكاديمي بفلتر صف+مجموعة) + الدرجات اليدوية + الحضور والاختبارات — كلها PDF
│  ├─ support.tsx          قناة الدعم: محادثة مباشرة مالك↔مطور (isOwner فقط)
│  ├─ whatsapp.tsx        نطاق (كل/صف/مجموعة/طالب) + هدف (طالب/ولي) + قوالب + إرسال منفصل (فشل رقم لا يعطل) + نطاق المدرس لمجموعاته
│  ├─ notifications.tsx   بث (عدّ دقيق بعضويات إضافية) + مقروءية باستعلام واحد + حذف
│  ├─ dev-notices.tsx     صندوق المطور (للمالك فقط)
│  ├─ announcements.tsx   بطاقات (مثبت مميز + عرض كامل) + مودال حديث
│  ├─ grades-list.tsx     إضافة + تعديل + حذف مع فك الارتباط (عرض فقط للفريق)
│  ├─ guide.tsx           دليل محدث (فريق/باقات/واتساب/اختبارات)
│  ├─ admin-settings.tsx  بيانات + اشتراك (planLabel) + تشغيلية (تحقق واتساب/بريد) + نسخ احتياطي كامل + (محجوب عن الفريق)
│  ├─ more.tsx            أقسام + كود + باركود السنتر (تصدير PNG باسم لاتيني) + روابط (فريق/إعدادات للمطور... للمالك فقط)
│  └─ student/[id].tsx    ملف شامل: مجموعات (أساسية+إضافية للمالك فقط) + مستحقات/دفعات (تحصيل collect) + درجات (grades) + محاولات بعناوين + سجل نشاط + تقرير PDF شامل + واتساب (notify)
└─ (student)/             تبويبات + شاشات مخفية (my-exams/my-inquiries/my-surveys/my-library/my-schedule/my-notifications)
   ├─ home.tsx            ترحيب + مجموعتي + باركود يومي + أوائل + شارة غير المقروء + روابط + إعلانات (20)
   ├─ my-exams.tsx        منشورة + تأدية الأنواع الثمانية بمؤقت (متعدد/وصل بخلط ثابت/أكمل بتطبيع قبل التسليم) + خروج مؤكد + نتائج (مراجعة)
   ├─ my-notifications.tsx غير مقروء/الأحدث + تعليم عند الفتح (UNIQUE يمنع التكرار)
   ├─ my-schedule.tsx     كل مجموعاته (أساسية+إضافية)
   ├─ my-library/surveys/inquiries/attendance/grades/payments/profile (اسم السجل أدق + تحميل آمن)

src/
├─ theme.ts               هوية «كربون المستقبل» (فحمي+نعناعي+كهرماني — داكن فقط) + ظلال
├─ components/            layout (BackHeader برجوع آمن canGoBack) · controls (AppButton/Content بأنيميشن spring + تباين أحبار + NoAccess + SheetHandle) · pickers (OptionPicker + DaysPicker + TimePicker + FormMessage بزر نسخ) · LoginForm (أدوار ثلاث + معلق + إعادة تأكيد + نسيت محمي) · DeveloperGate (صفحة لا Layout) · ConnectionSetup · UpdateManager (تباين داكن)
└─ lib/
   ├─ types/api/supabase/session/config/pendingRegistration (سنتر/طالب/فريق)
   ├─ rbac.ts             المنطق النقي القابل للاختبار في Node: STAFF_ROLES + isStaff/isOwner + can() + roleLabel + TEACHER_TABS/SCREENS
   ├─ staff.ts            غلاف يعيد تصدير rbac + useTeacherGroupIds (Hook نطاق مجموعات المدرس)
   ├─ billing.ts          PRODUCTS (الأسعار والحدود) + planLabel + limitsFor + priceFor + TRIAL_DAYS=14
   ├─ qr.ts               يومي MRC1 + ثابت MRC0 (XOR+FNV + UTF-8 عربي + يومية تمنع السكرين)
   ├─ whatsapp.ts         wa.me + تطبيع مصري + قوالب
   ├─ push.ts             رمز الدفع صامتاً (بعد فحص projectId أولاً حتى لا يزعج)
   ├─ backup.ts           نسخة JSON كاملة (23 جدولاً) بالتوازي + مشاركة
   └─ report.ts           HTML عربي → PDF ومشاركة

scripts/  test-fresh (منظومة مستقلة: باقات/مصفوفة صلاحيات/بوابات شاشات/ثيم حي/بث مجمّع/rate limit/تنقل/عزل api/مخطط) · test-utils (utils+billing) · test-qr · test-worker (يشمل /push/notify 403/400) · audit-sql · audit-rpc (تطابق وسائط التطبيق↔القاعدة) · audit-app · test-e2e-live (حي: E2E_* keys + تنظيف + تدوير service_role بعده)
```

**اصطلاحات**: `getSupabase()` فقط · لا حالة خارج SessionProvider · الأدوار من `profiles` · أخطاء معربة · RLS أولاً والواجهة تجميل · أي شاشة جديدة تُسجل في (الحارس + التبويبات + audit-app) معاً · **التغيير في المخطط/الصلاحيات/Data يُنشر في مستودع الويب معاً**.

### الملفات المشتركة مع الويب (منطق نقي متطابق)
| الملف | الدور | الوجهان |
|---|---|---|
| `android_multitenant_schema.sql` | المخطط الكامل + RLS + RPC | ويب ∧ موبايل |
| `cloudflare/worker.js` | الوسيط (مفاتيح + تحديث + دفع) | ويب ∧ موبايل |
| `src/lib/rbac.ts` | منطق الصلاحيات النقي | ويب ∧ موبايل |
| `src/lib/billing.ts` | الباقات والأسعار والحدود | ويب ∧ موبايل |
| `src/lib/types.ts` | الأنواع (متطابقة) | ويب ∧ موبايل |
| `src/lib/utils.ts` | دوال مساعدة نقية | ويب ∧ موبايل |
| `src/lib/qr.ts` | ترميز/فك QR | ويب ∧ موبايل |
| `src/lib/whatsapp.ts` | قوالب وروابط واتساب | ويب ∧ موبايل |

---

## 8) تدفقات الاستخدام الحرجة

- **تسجيل سنتر/منفرد**: نوع الحساب + البيانات + فحص الكود/التوفر + `signUp` ← جلسة؟ فوراً (`complete_center_registration` + تجريبي trial 14 يوماً) : معلق + تأكيد + دخول فيُستكمل ← الحارس → `/dashboard`.
- **تسجيل طالب**: كود/باركود (موقوف=مرفوض) ← بطاقة السنتر ← صف/مجموعة (قوائم آمنة) ← توفر ← `signUp` ← فوراً/معلق ← ربط بسجل هاتف موجود إن وُجد ← `/home`. الكود لا يُطلب بعدها أبداً.
- **تسجيل فريق**: كود + صفة (مدرس/مدير/سكرتير) ← حساب **خامل** ← المالك يفعّل + صلاحيات + مجموعات (بحدود الباقة خادمياً).
- **الدخول**: بريد+كلمة ← فحص الدور الفعلي + `is_active` + مطابقة النوع مع الشاشة (للمعلق خصوصاً) ← استكمال المعلق (مع الاحتفاظ به عند الفشل) ← الحارس (اشتراك/إيقاف/موقوف) ← التوجيه. الموقوف إدارياً يُطرد فوراً.
- **طلب ترقية**: المالك (صفحة الباقات) ← خطة+مدة+تاريخ وقيمة التحويل ← `pending` ← المطور يعتمد فيُنشأ الاشتراك + سجل ← أو يرفض. السحب متاح للمعلق.
- **تغيير القاعدة/إصدار تحديث/RouterGuard**: كما كانت (استبعاد `(` من المسارات؛ المسارات الفعلية محدثة في `_layout` و`audit-app`).

---

## 9) الثوابت التي يُمنع كسرها

يحميها `npm test` آلياً — قبل أي تعديل تحقق:
1. لا RLS واسعة `USING (true)` أبداً.
2. العزل خادمي: أي كيان متعدد السناتر = `center_id` + سياسات + تسجيل في `audit-sql`.
3. كود السنتر ثابت وفريد؛ status للمطور (مشغّلات).
4. الطالب قراءة نفسه فقط عبر `my_student_id()`؛ الامتحانات والمحاولات المباشرة والإشعارات عبر RPC فقط.
5. القائمة البيضاء `legacy_admins` فقط — ممنوع «بلا ملف = أدمن».
6. البريد/الهاتف فريدان عالمياً + موفرين معروفين + ولي مختلف (عميل وخادم).
7. أسماء الموقع المشتركة لا تتغير نوعها؛ زيادات بأعمدة فقط.
8. لا تُعِد تشغيل مخطط الموقع القديم بعد مخططنا أبداً.
9. `extra.configUrl` ثابت وحيد — تغييره = إصدار جديد.
10. RTL والعربية + تباين نصوص التدرجات الداكنة (`#052E22` على النعناعي).
11. الإصدار: `app.json` + العامل معاً (فحص توافق آلي).
12. `npm test` + `typecheck` + `expo export` قبل الرفع.
13. أي RPC تُغيَّر وسائطه/إرجاعه: `DROP FUNCTION` أولاً + تعريف الدوال قبل سياساتها + فحص `audit-rpc` أخضر.
14. المنع الافتراضي للفريق: أي شاشة/زر جديد يُفحص بـ `can()` أو `isOwner` + يُسجل في `TEACHER_SCREENS`/التبويبات + فحص `audit-app`.
15. أرقام الباقات والحدود في `billing.ts` فقط — يغطيها `test-utils` (أي تغيير سعر يكسر الفحص عمداً للمراجعة).
16. أنواع أسئلة الامتحان ثمانية في `types.ts`/`EXAM_TYPE_LABEL` — التصحيح التلقائي خادمي بمساواة JSON عامة (multi=مصفوفة مرتبة · complete=نص عبر `normalizeAnswerText` · match=مصفوفة هوية)، واليدوي (correct/essay/short) يذهب لـ`pending_review` عبر `v_type IN ('essay','correct','short')` في الـRPC. «صحّح» بنموذج يُحسب آلياً عند المطابقة التامة.
17. نطاق المدرس: students/groups/schedule (وكل شاشة بيانات) تقصر قوائمها على `useTeacherGroupIds` — المدرس لا يرى إلا مجموعاته المسندة وطلابها. ملف الطالب: المستحقات والدفعات بصلاحية collect/reports فقط. guide/schedule عرضيتان (قراءة فقط) بلا بوابة صلاحية.
18. **تكافؤ الويب والموبايل**: الويب والتطبيق يقرآن ويكتبان على نفس الجداول ونفس RPC ونفس سياسات RLS. لا يوجد منطق بيانات مختلف بين الوجهين — الاختلاف فقط في طبقة العرض (UI). أي تغيير في المخطط أو الصلاحيات أو البيانات يجب أن يُنشر في المستودعين معاً. لا أسرار في الكود/الشات (`service_role` للعامل Secrets والسكريبتات فقط، ويُبدَّل بعد المشاركة).
16. لا أسرار في الكود/الشات (`service_role` للعامل Secrets والسكريبتات فقط، ويُبدَّل بعد المشاركة).

---

## 10) الاختبارات والتحقق

| الجناح | الفحوص | الوصف |
|---|---|---|
| `test-fresh.mjs` | 186 | منظومة مستقلة كلياً: باقات حرفية · مصفوفة صلاحيات كل دور · بوابات كل شاشة · نطاق المدرس · ثيم حي بتباين WCAG · تجميع بث المطور · rate limit بكل الصيغ · بنية تنقل · عزل api · مخطط القاعدة |
| `test-utils.mjs` | 119 | منطق نقي (utils+billing بتجميع tsc): تطبيع/صيغ/وقت/تعارض/بريد/روابط/امتحانات (8 أنواع + normalizeAnswerText + seededShuffle)/باقات وأسعار |
| `test-qr.mjs` | 15 | ترميز/فك الطالب والسنتر (يشمل عربياً) + رفض الدخيل/المعبث + اليومية |
| `test-worker.mjs` | 22 | مسارات العامل + CORS + توافق الإصدار + `/push/notify` (403/400) |
| `audit-sql.mjs` | 117 | بنية/RLS/عزل/قائمة بيضاء/مشغلات/فرادة/RPC/جداول جديدة/ترتيب التعريف/DROPs |
| `audit-rpc.mjs` | 30 | تطابق كل `rpc()` في التطبيق مع تعريفات المخطط وسيطاً وسيطاً (يكشف قاعدة منشورة أقدم) |
| `audit-app.mjs` | 113 | إعدادات/تبعيات/بوابات/خرائط المدرس/شاشات/حارس/تحديث/كاميرا/واتساب/بث |

+ `typecheck` صارم + `expo export --platform android`. **الأخضر الكلي الحالي: 392.**
+ **الحي** `npm run test:e2e` (منفصل): زائر/مالك/طالبان/سنتر ثان/مدرس/مطور/بوابة/طلبات — يتطلب `E2E_SUPABASE_URL/ANON_KEY/SERVICE_KEY` + تشغيل المخطط أولاً، وينظف وراءه، و**بدّل service_role بعده**.

### اختبارات الويب (في مستودع `mr-center-web`)
مستودع الويب له أجنحته الخاصة التي تتحقق من نفس المخطط ونفس المنطق:
- `npm test` في الويب = `typecheck` + `test-pure` + `check-routes` + `audit-code` + `audit-security` + `audit-accounting`
- `audit-sql.mjs` في الويب: يتحقق من نفس المخطط ونفس RLS
- `compare-android-parity.mjs`: يقارن بنية المخطط بين الويب والموبايل ويكشف التفاوت
- `e2e-live.mjs`: اختبار حي على القاعدة الحقيقية (مثل موبايل)

---

## 11) البناء والتوزيع والتحديثات

### الموبايل
- `npm install && npx expo start` (عند كاش قديم: `-c`) ← Expo Go. **الثيم/الكود الجديد لا يظهر في APK مثبت قديم إطلاقاً — يلزم بناء جديد.**
- APK: `eas build --platform android --profile preview` · AAB: `--profile production` (نفس الشهادة شرط التحديث-فوق-القديم).
- التحديث الذاتي عبر R2+العامل. iOS مستقبلاً عبر المتجر.
- الدفع الفوري: مشروع EAS (`eas init` لمعرف المشروع) + Secrets العامل + Cron — بدونه الإشعارات داخل التطبيق فقط.

### الويب
- `npm install && npm run dev` ← Next.js على `http://localhost:3000`.
- النشر على Vercel: اربط المستودع بدون Environment Variables (يقرأ من كلاود فلير).
- Build Command: `npm run build` — Output تلقائي من Next.js.
- في Supabase Auth: اضبط Site URL على دومين Vercel + Redirect URLs.

---

## 12) منصة الويب — الوجه الثاني للمنتج

> **الويب والتطبيق منصة واحدة بوجهين — ليسا مشروعين منفصلين.**

### المبادئ الحاسمة

1. **نفس كل شيء**: نفس قاعدة البيانات، نفس الجداول، نفس الأعمدة، نفس سياسات RLS، نفس دوال RPC، نفس المشغّلات، نفس التسعير والباقات، نفس الصلاحيات، نفس المستخدمين. الحساب الذي يسجّل من الموبايل يدخل من الويب بنفس البريد ونفس الصلاحيات ويرى نفس البيانات فوراً.
2. **لا تكرار في منطق البيانات**: منطق البيانات (أنواع، تحقق، تسعير، صلاحيات، RPC) موجود في كل مستودع بشكل متطابق — لكن كل مستودع يطبقه بأدوات منصته (React Native مقابل React/Next.js). التحديث في المنطق يجب أن يُنشر في المستودعين معاً.
3. **المخطط واحد**: ملف `supabase/android_multitenant_schema.sql` يُشغَّل مرة واحدة على مشروع Supabase ويخدم الويب والموبايل. لا يوجد «مخطط ويب» و«مخطط موبايل» — كلاهما يقرأ نفس الملف.
4. **كلاود فلير واحد**: العامل (`cloudflare/worker.js`) يقرأه الويب والموبايل. تغيير الرابط أو المسارات أو الأسرار يؤثر على الوجهين.
5. **التحقق على الوجهين**: أي تغيير في RLS أو RPC أو المخطط يُختبر على الويب والموبايل (حيث أمكن) أو يُراجع يدوياً للتأكد من عدم كسر الوجه الآخر.

### بنية منصة الويب

```
app/page.tsx              الصفحة الرئيسية (ترحيب + دخول + تسجيل)
app/layout.tsx            الجذر: SessionProvider + CookieConsent + VisitorTracker
app/globals.css           هوية «كربون المستقبل» المتحركة (CSS variables)
app/auth/                 login/register-center/register-student/register-staff/update-password
app/admin/                لوحة المالك والفريق (نفس شاشات الموبايل بالمسميات الآلية)
  ├─ layout.tsx           RequireAuth(roles) + AppFrame(admin)
  ├─ dashboard/students/groups/attendance/payments/grades/...
  ├─ student/[id]         ملف الطالب
  └─ accounting/custody/subscription/...
app/student/              لوحة الطالب
  ├─ layout.tsx           RequireAuth(student) + AppFrame(student)
  └─ home/exams/payments/profile/notifications/...
app/developer/            لوحة المطور
  ├─ layout.tsx           RequireAuth(super_admin) + DeveloperGate
  └─ centers/subscriptions/broadcast/support/app-info/connection
app/blocked/page.tsx      حظر الاشتراك
app/about/page.tsx        حول المنصة
app/api/                  مسارات API (تجديد الجلسة بـ HttpOnly cookies)
```

### الملفات المشتركة بين الويب والموبايل (منطق نقي متطابق)
| الملف | الدور | الوجهان |
|---|---|---|
| `android_multitenant_schema.sql` | المخطط الكامل + RLS + RPC | ويب ∧ موبايل |
| `cloudflare/worker.js` | الوسيط (مفاتيح + تحديث + دفع) | ويب ∧ موبايل |
| `src/lib/rbac.ts` | منطق الصلاحيات النقي | ويب ∧ موبايل |
| `src/lib/billing.ts` | الباقات والأسعار والحدود | ويب ∧ موبايل |
| `src/lib/types.ts` | الأنواع (متطابقة) | ويب ∧ موبايل |
| `src/lib/utils.ts` | دوال مساعدة نقية | ويب ∧ موبايل |
| `src/lib/qr.ts` | ترميز/فك QR | ويب ∧ موبايل |
| `src/lib/whatsapp.ts` | قوالب وروابط واتساب | ويب ∧ موبايل |

### الاختلافات المقبولة (طبقة العرض فقط)
- الموبايل يستخدم `expo-router` + ملفات `app/(x)/_layout.tsx` للتبويبات؛ الويب يستخدم `app/(x)/layout.tsx` لـ Next.js App Router.
- الموبايل يستخدم `AsyncStorage` للثيم والجلسة؛ الويب يستخدم `localStorage` + HttpOnly cookies لرمز التجديد.
- الموبايل يستخدم `expo-notifications` للدفع الفوري؛ الويب يستخدم Supabase Realtime + عامل كلاود فلير.
- الموبايل يبني APK/AAB عبر EAS؛ الويب ينشر على Vercel.
- الموبايل يستخدم `expo-camera` لمسح QR؛ الويب يستخدم كاميرا المتصفح عبر `navigator.mediaDevices`.

### قواعد التعديل المشترك
- **عند إضافة جدول/عمود/RPC جديد**: أضفه في `android_multitenant_schema.sql` ثم حدّث الأنواع في المستودعين معاً.
- **عند تغيير صلاحية أو دور**: حدّث `rbac.ts` في المستودعين + السياسات في المخطط + اختبارات الوجهين.
- **عند تغيير سعر أو باقة**: حدّث `billing.ts` في المستودعين + اختبارات الأسعار.
- **عند إضافة شاشة ويب جديدة**: تأكد أن المسار والبيانات والصلاحيات متطابقة مع شاشة الموبايل المقابلة.
- **لا تكسر الويب من أجل الموبايل والعكس** — التوافق إلزامي.

---

## 13) بيئة العمل وخبرات عملية مهمة

### الموبايل
- **Node 24 / npm**؛ سجل npm يعمل؛ **expo API محجوبة** — ثبّت يدوياً بنسخ SDK 52 المتوافقة: `expo-camera@^16` · `react-native-svg@^15` · `react-native-qrcode-svg@^6` · `expo-print@^14` · `expo-sharing@^13` · `expo-clipboard@^7` · `expo-notifications@~0.29.14` (نسخ 0.x لجيل 52 — لا توجد 3/4!). تحقق من `npm view` عند الشك.
- لا Android SDK/Java — التحقق `tsc` + `expo export` + ساكن؛ APK عبر EAS عند المستخدم.
- `node_modules` لا تُحفظ بين الجلسات — ابدأ بـ `npm install`. **لا تنفذ `npm audit fix`** — يكسر تثبيتات Expo.
- Metro: أي `SyntaxError` عابر بعد تعديل = كاش قديم غالباً (`-c`)؛ تحقق أولاً من توازن الأقواس.
- `getExpoPushTokenAsync` يحتاج `extra.eas.projectId` — بدونه يتخطى صامتاً.

### الويب
- **Node 24 / npm**؛ نفس بيئة الموبايل الأساسية. Next.js 16 + TypeScript + React 19.
- الويب يقرأ مفاتيح قاعدة البيانات من كلاود فلير (مثل الموبايل تماماً) — لا يحتاج Environment Variables.
- `NEXT_PUBLIC_CONFIG_URL` اختياري لتغيير خادم الإعدادات عن الافتراضي.
- **لا تضع أبداً `service_role` في متغير يبدأ بـ `NEXT_PUBLIC_`** — الحماية كلها في RLS.
- جلسة واحدة لكل حساب: `claimMySession` + `check_session` يضمنان أن آخر جهاز يدخل يستحوذ على الجلسة.

### المشترك بين الوجهين
- PowerShell 5.1: بلا `&&` (استخدم `;`)، وانتبه لترميز الكتابة (فضّل أدوات التحرير على Set-Content).
- فرع العمل **`arena/01a08697-mr-center-android`** (موبايل) — ادفع إليه فقط.
- لا تضع أسراراً في الريبو (`.npmrc` و`.wrangler/` و`CODELY.*` غير مُتتبعة — أبقها كذلك).
- بعد كل تعديل: `npm run typecheck && npm test` في كل مستودع.

---

## 14) قيود معروفة وخطط مستقبلية

- الدمج بالهاتف: من يعرف هاتف طالب + الكود قد يرتبط بسجله — المسئول يرى البريد المرتبط ويصحح.
- التسعير الأسبوعي = السعر ×4 ثابت؛ المتوسطات غير مرجحة زمنياً؛ `check_availability` كاشفة جزئياً (مقصودة + حدود معدل).
- الإتاحة الزمنية: `formatDate` بتوقيت الجهاز (مصر آمنة)؛ تذكير العامل بتوقيت UTC+إزاحة (راجع DST صيفاً/شتاءً).
- نسخة APK المثبتة لا تتحدث ذاتياً قبل إعداد R2+UPDATE.
- حسابات الفريق المحذوفة تبقي حساب Auth يتيماً (بلا ملف → «غير مكتمل» عند الدخول) — يُحذف من لوحة Auth عند اللزوم.
- ترقية الموقع لاحقاً + iOS عبر المتجر + Webhook فوري للبث (موثق، يدوي الإعداد).

---

## 15) قاموس مصطلحات المالك

| يقصد | المعنى عندنا |
|---|---|
| «المنصة» أو «النظام» | منصة Mr Center كاملة — ويب + موبايل معاً كمنتج واحد بوجهين |
| «الموبايل» أو «التطبيق» | تطبيق الجوال (Expo / React Native) — الوجه الأول |
| «الويب» أو «الموقع» | منصة الويب (Next.js) — الوجه الثاني |
| «نفس البيانات» | ويب ⇄ موبايل = نفس الجداول ونفس RLS ونفس المستخدمين |
| «السنتر» | المركز المتكامل (`kind='center'`) — بفريق تابع |
| «مدرس خصوصي/منفرد» | (`kind='solo'`) — نفس الوظائف بلا فريق، حتى 200 طالب |
| «كود السنتر» | `code` 3–8 ثابت وفريد + باركود MRC0 للطباعة |
| «فريق العمل/المدرس» | `teacher/manager/secretary` — خامل حتى التفعيل + 10 صلاحيات + مجموعات مسندة |
| «المدير/السكرتير» | أدوار فريق بحدود الباقة (1/1-2/2-4) تُفرض خادمياً |
| «الباقة/الترقية» | trial(14 يوماً) → center_full / center_medium / solo_teacher — طلب بتحويل يعتمده المطور |
| «سجل العمليات/المعاملات» | `activity_log` / `subscription_requests` |
| «الإشعار/البث» | داخلي مجاني (صف/رسالة + مقروءية) + فوري عبر العامل (push/cron) |
| «سوبر أدمن / المطور» | `super_admin` — المالك نفسه |
| «مفاتيح الربط» | url + anon_key |
| «كلاود فلير» | العامل (`worker.js` + R2 + Secrets + Cron) |
| «تحديث التطبيق» | فحص الإصدار ← تحميل APK ← تثبيت فوق القديم |

---

## 16) قائمة فحص قبل التسليم

- [ ] `npm test` — الأجنحة السبعة خضراء (وأضِف فحوصاً لما أضفته).
- [ ] `npm run typecheck` — صفر أخطاء.
- [ ] `CI=1 npx expo export --platform android` — الحزمة تُبنى.
- [ ] القسم 9 بنداً بنداً + العربية RTL + رسائل معربة بزر نسخ.
- [ ] أي شاشة/دور/جدول/RPC جديد مسجل في (الحارس + التبويبات + audit-app/audit-sql/audit-rpc).
- [ ] **تكافؤ الويب**: أي تغيير في المخطط أو الصلاحيات أو البيانات أو RPC مُنشر في مستودع الويب معاً (`mr-center-web`).
- [ ] AGENTS.md + README محدثان لو تغيّر سلوك أو إعداد.
- [ ] كوميت عربي واضح + دفع لفرع العمل نفسه + بلا أسرار.


---

## 17) سجل تغييرات جلسة Arena الحالية — سبتمبر 2026

هذا القسم أُضيف حتى لا يعتمد الوكيل التالي على ادعاءات قديمة في أعلى الملف.

### تعديلات التطبيق الفعلية
- `app/auth/register-teacher.tsx`: أُلغي التسجيل الذاتي للمدرس/المدير/السكرتير. الصفحة الآن توضح أن إضافة الحساب تتم حصراً من صاحب السنتر من شاشة فريق العمل. لا تعيد إضافة `registerStaffAccount` إلى هذه الشاشة إلا إذا تغيرت المواصفة.
- `app/index.tsx`: استُبدل زر «انضمام لفريق سنتر» بزر معلومات يوجه لتوضيح أن صاحب السنتر هو من ينشئ الحساب.
- `app/(admin)/(tabs)/dashboard.tsx`: رسالة الترحيب تعرض `profile.full_name`، وأضيف وصول مباشر للإشعارات والرسائل/الاستفسارات.
- `app/(admin)/(tabs)/more.tsx`: أضيف رابط «الحسابات» لصاحب السنتر فقط.
- `app/_layout.tsx`: أضيف مسار `accounting` إلى قائمة مسارات الإدارة المسموحة.
- `app/(admin)/accounting.tsx`: شاشة حسابات محمية بـ `isOwner`: ملخص إيرادات/مصروفات/صافي، تسجيل المصروفات العامة والرواتب والسلف والمكافآت والإيجار والمرافق والمشتريات، اختيار موظف، تجميع تحصيل كل محصل، وتجميع مصروفات الموظفين. إيرادات الطلاب لا تُدخل يدوياً.
- `src/lib/api.ts`: `recordPayment` يرسل `collected_by` و`collected_by_name` مع الدفع.
- `scripts/audit-app.mjs`: فحص التسجيل عُدّل ليعتبر منع التسجيل الذاتي هو السلوك الصحيح.

### تعديلات قاعدة البيانات الفعلية
كلها أُضيفت إلى نهاية `supabase/android_multitenant_schema.sql`، ولا يوجد ملف migration منفصل حالياً:
- `center_subscriptions.extra_teachers/extra_secretaries/extra_managers` و`enabled_features`.
- `center_ledger` مع RLS، أنواع الحركة، الموظف، الشهر/السنة، الخصم، ومصدر الدفع.
- Trigger `record_payment_income` ينشئ إيراداً تلقائياً من كل دفعة جديدة ويمنع التكرار بـ `source_payment_id`.
- `center_entitlements` لدعم زيادة أعداد الفريق لكل سنتر مع `starts_on`, `ends_on`, `is_open_ended`.
- RPC `dev_upsert_entitlement` للمطور، لكنها تحتاج واجهة مطور مرئية لم تُنجز بعد.
- Trigger `staff_limit_check` في آخر المخطط يأخذ زيادات الاشتراك، لكنه لا يقرأ `center_entitlements` بعد؛ يجب على الوكيل التالي دمج الاثنين قبل الإنتاج حتى تُفرض الزيادة المؤقتة خادمياً.

### التحقق المنفذ
- `npm ci --ignore-scripts` تم بنجاح.
- `npm run typecheck` يمر دون أخطاء.
- `npm test` يمر بنتيجة `136 فحص ناجح / 0 فاشل` في الحالة الحالية.
- هذه الاختبارات ساكنة/محلية وليست دليلاً على تشغيل Supabase حقيقي أو جهاز Android حقيقي.

### ما لم يُنفذ ولا يجوز وصفه بأنه مكتمل
1. واجهة المطور المرئية لتعديل الزيادات، وتاريخ البداية/النهاية، والمفتوح بلا نهاية.
2. ربط `center_entitlements` فعلياً داخل `staff_limit_check`.
3. كشف راتب PDF شهري احترافي مع إجمالي/خصومات/سلف/صافي لكل موظف.
4. كشف عهدة يومي/شهري للسكرتير ومطابقته بالتحصيل والتسليم.
5. تقارير أرباح وخسائر بفلاتر تاريخية حقيقية من واجهة الحسابات.
6. إضافة اسم منفذ التقرير/الطباعة ديناميكياً في كل قوالب PDF؛ `src/lib/report.ts` موجود لكنه يحتاج `generatedBy` صريحاً واستدعاءات من الشاشات.
7. تشغيل SQL على مشروع Supabase فعلي والتحقق من RLS/RPC بعد الترحيل.
8. اختبار E2E فعلي بمستخدمين من سنترين مختلفين؛ استخدم `npm run test:e2e` بعد توفير متغيرات `E2E_*` ولا تضع المفاتيح في Git.
9. بناء/تجربة APK على جهاز Android حقيقي؛ البيئة الحالية لا تحتوي Android SDK/جهاز.
10. تحديث أرقام/عبارات AGENTS القديمة التي تدّعي 392 فحصاً أو E2E حي ناجحاً؛ الرقم المثبت في هذه الجلسة 136/0 فقط.

### تعليمات الوكيل التالي
- لا تدّعِ أن migration طُبق على Supabase دون مخرجات SQL فعلية من المشروع المستهدف.
- قبل استخدام `center_ledger` في الإنتاج، شغّل migration على قاعدة اختبار وتأكد أن `payments.center_id` موجود وأن trigger يرى `students.center_id`.
- عند إضافة شاشة جديدة حدّث RouterGuard و`audit-app` وRLS/`audit-sql`.
- لا تستعمل `service_role` في التطبيق. مفاتيح E2E/Cloudflare تبقى خارج Git.
- بعد كل تعديل شغّل `npm run typecheck && npm test`.

### جولة 9/12/2026 — محاسبة كخدمة مدفوعة + سنوات مالية + دعوات الفريق + إصلاحات
- **عهدة التحصيل**: سبب إعادتها للرئيسية كان غياب `custody` من قائمة الحارس — أُضيف. المالك لا يراها إلا عند تفعيل المحاسبة.
- **المحاسبة كخدمة مدفوعة**: دالة `accounting_enabled(uuid)` + سياسة `ledger_owner_all` مشروطة بها (خادمياً). دون التفعيل: الإيرادات تُسجل بالخلفية عبر trigger (SECURITY DEFINER) ويراها المطور فقط؛ وعند التفعيل يجد المالك سجل التحصيل كاملاً منذ البداية. مفتاح التفعيل/التعطيل في «ملف السنتر» بلوحة المطور (`dev_set_entitlements` مع `enabled_features.accounting`)، وحراس الواجهة في accounting/more.
- **السنوات المالية**: جدول `center_fiscal_years` + RPC `open_fiscal_year`/`close_fiscal_year` (الإغلاق يحسب الصافي، يغلق السنة، ويفتح التالية برصيد مرحّل تلقائياً) + قسم كامل في شاشة الحسابات.
- **المدير الوحيد**: `staff_limit_check` يمنع المدير الإضافي (`lim=0` — يُضاف فقط عبر زيادات المطور)، و`billing.ts` limits managers=0، وفحوص الأسعار محدّثة.
- **دعوات فريق العمل**: جدول `staff_invites` (سكرتير/مدرس فقط) + RPC `get_invite_info`/`accept_staff_invite` + زر «دعوة موظف» في شاشة الفريق بكود قابل للنسخ والسحب + إعادة كتابة `register-teacher` لتدفق الكود (كود ← بريد/كلمة ← تأكيد ← أول دخول يُكمل عبر `accept_staff_invite`). `pendingRegistration` للفريق صار `inviteCode`.
- **الوضع الفاتح/الداكن**: كان `SessionProvider key={mode}` يعيد تحميل الجلسة بالكامل عند التبديل — أُزيل المفتاح والتبديل الآن فوري عبر سياق الثيم.
- **جولة تسويق الحسابات + عزل المجموعات**: أُزيل اختيار المدرس من نموذج المجموعة (التعيين حصراً من شاشة فريق العمل/إسناد المجموعات)، ووُضع تنبيه في النموذج. قسم الحسابات: لغير المشترك صفحة تسويقية كاملة (مالا المزايا/التقارير/ما يستفيد + زر للباقات)، وللمشترك زر صغير «معلومات حول القسم» يفتح شرحاً تفصيلياً. القسم ظاهر لصاحب السنتر فقط (واجهة + RLS).
- الحالة: `typecheck` صفر أخطاء + `npm test` 645 فحصاً/0 فاشل + `expo export` يبني. لم يُنفَّذ SQL على Supabase حقيقي ولا اختبار جهاز حقيقي بعد.

### جولة 14/9/2026 — إصلاح انحدار تصحيح «صحّح الخطأ» + تحديث توقعات E2E القديمة
سبب هذه الجولة: CI شغّل `npm run test:e2e` فعلياً (أول مرة توجد له مخرجات حقيقية بأسرار E2E_* في
GitHub Actions) وأظهر 5 فشل من 67. التحقيق كشف أن **فشل واحد فقط كان انحداراً حقيقياً في SQL
الإنتاج**، والباقي أربعة كانت توقعات قديمة في `scripts/test-e2e-live.mjs` لم تُحدَّث بعد تغييرات
قواعد عمل شرعية أُدخلت في جولات سابقة (`already_attempted` صار `attempts_exhausted` بعد السماح
بعدة محاولات، «حد المدير» صار صفراً دائماً بعد قرار «المدير الوحيد»، عمود `dues.year` صار
`dues.due_year`، وطلب `kind='transfer'` صار يتطلب `from_group_id`/`to_group_id`).

- **إصلاح فعلي (SQL)**: `supabase/20260913_exam_workspace.sql::submit_exam_attempt` — عند دمج
  مسار ورقي/إلكتروني في هذا الملف، سقط فرع «صحّح الخطأ بمطابقة تامة = درجة آلية» الموجود في
  `20260912_exams_complaints.sql` والمخطط الأساسي، فصار كل سؤال من نوع `correct` يذهب دائماً
  لمراجعة المعلم اليدوية حتى لو طابق الطالب النموذج حرفياً. بما أن هذا الملف يُشغَّل بعد
  `20260912_exams_complaints.sql` في ترتيب README فهو يفوز، وأي قاعدة حية طبّقت هذا الملف تحمل
  هذا الانحدار فعلياً. إن كانت لديك قاعدة حية شغّلت `20260913_exam_workspace.sql` قبل هذا
  الإصلاح، **أعد تشغيل نسخته المحدّثة** (idempotent، `CREATE OR REPLACE FUNCTION` فقط، لا يفقد بيانات).
- **تحديثات E2E فقط (لا تغيير سلوك تطبيق)**: `scripts/test-e2e-live.mjs` — الاستفسار التجريبي صار
  `kind='question'` بدل `'transfer'` الناقص الحقول، توقع تكرار المحاولة صار `attempts_exhausted`،
  إدخال `dues` صار بعمود `due_year`، وتفعيل «مدير» إضافي صار يتوقع الرفض دائماً (`staff_limit_reached`)
  بدل النجاح.
- **الحالة بعد الإصلاح**: `npm run typecheck` صفر أخطاء، `npm test` 136/136 (محلي، بلا Supabase حي)،
  `node --check scripts/test-e2e-live.mjs` سليم. **لم يُعَد تشغيل `test:e2e` فعلياً على قاعدة حية
  في هذه الجلسة** — التحقق النهائي من الـ67 فحصاً كاملة يحتاج CI الحقيقي (GitHub Actions مع أسرار
  E2E_*) أو تشغيل يدوي بمتغيرات البيئة الثلاثة.
