# 🤖 AGENTS.md — الملف التوجيهي الشامل لمشروع Mr Center

> **هذه الوثيقة مكتوبة لوكيل الذكاء الاصطناعي الذي سيعمل على هذا المشروع.**
> اقرأها كاملة قبل لمس أي سطر. هي المرجع النهائي لفهم المشروع من أوله لآخره:
> ماذا يريد المالك، كيف بُني، لماذا اتُّخذت كل قرار، وما الذي يُمنع كسره.
>
> آخر تحديث: سبتمبر 2026 — الحالة: مشروع مكتمل ومُختبر (161 فحصاً آلياً ناجحاً).

---

## جدول المحتويات

1. [الهوية والرؤية](#1-الهوية-والرؤية)
2. [ما يريده المالك حرفياً (متطلبات مُلزِمة)](#2-ما-يريده-المالك-حرفيا-متطلبات-مُلزِمة)
3. [الخريطة المعمارية الكاملة](#3-الخريطة-المعمارية-الكاملة)
4. [نموذج تعدد السناتر والصلاحيات](#4-نموذج-تعدد-السناتر-والصلاحيات)
5. [قاعدة البيانات بالتفصيل](#5-قاعدة-البيانات-بالتفصيل)
6. [نظام كلاود فلير (الوسيط المركزي)](#6-نظام-كلاود-فلير-الوسيط-المركزي)
7. [بنية كود التطبيق ملفاً ملفاً](#7-بنية-كود-التطبيق-ملفاً-ملفاً)
8. [تدفقات الاستخدام الحرجة](#8-تدفقات-الاستخدام-الحرجة)
9. [الثوابت التي يُمنع كسرها](#9-الثوابت-التي-يُمنع-كسرها)
10. [الاختبارات والتحقق](#10-الاختبارات-والتحقق)
11. [البناء والتوزيع والتحديثات](#11-البناء-والتوزيع-والتحديثات)
12. [علاقة التطبيق بموقع الويب القديم](#12-علاقة-التطبيق-بموقع-الويب-القديم)
13. [بيئة العمل وخبرات عملية مهمة](#13-بيئة-العمل-وخبرات-عملية-مهمة)
14. [قيود معروفة وخطط مستقبلية](#14-قيود-معروفة-وخطط-مستقبلية)
15. [قاموس مصطلحات المالك](#15-قاموس-مصطلحات-المالك)
16. [قائمة فحص قبل التسليم](#16-قائمة-فحص-قبل-التسليم)

---

## 1) الهوية والرؤية

- **المنتج**: تطبيق جوال لإدارة «السناتر» التعليمية المصرية (الدروس الخصوصية) — طلاب، مجموعات، حضور، مدفوعات، درجات، إعلانات.
- **التقنية**: React Native + Expo SDK 52 + TypeScript + expo-router v4. يعمل على أندرويد الآن، وعلى iOS لاحقاً بنفس الكود.
- **اللغة**: واجهة عربية 100% باتجاه RTL مفروض، وتعليقات الكود والتوثيق بالعربية. المالك يتكلم عربية (لهجة مصرية) — خاطبه بها.
- **القاعدة**: Supabase (PostgreSQL + Auth + RLS) — **نفس مشروع Supabase الذي يستخدمه موقع الويب الحالي للمالك**، بحيث تظهر البيانات متطابقة بين التطبيق والموقع لأنهما على نفس الجداول.
- **الوسيط**: عامل Cloudflare (Worker) هو نقطة التحكم المركزية لمفاتيح القاعدة وللتحديثات (راجع القسم 6).

### المستودعات المرتبطة
| المستودع | الدور |
|---|---|
| `contashepo-create/Mr-center-android-` (هذا) | تطبيق الجوال + مخطط SQL + عامل كلاود فلير + الأدلة |
| `contashepo-create/centerpuplish` | موقع الويب الحالي (Next.js + Supabase، أحادي المستأجر قديماً) — سيُرقَّى لاحقاً ليواكب هذا المخطط |

- فرع العمل في هذا المستودع: **`arena/01a08697-mr-center-android`** — اعمل عليه فقط وادفع إليه فقط.

---

## 2) ما يريده المالك حرفياً (متطلبات مُلزِمة)

هذه ليست اقتراحات — هي مواصفة المنتج. أي تغيير مستقبلي يجب أن يحافظ عليها:

1. **تطبيق أندرويد كامل احترافي** يعمل على كل الأجهزة وأحجام الشاشات، بتصميم حديث متدرج الألوان، انتقالات سلسة، وشاشات لكل قسم.
2. **الربط بنفس قاعدة Supabase المربوطة بالموقع** — حتى لو أُغلق الموقع لأي سبب، يعمل التطبيق مستقلاً بالكامل.
3. **نظام صلاحيات من ثلاث فئات**:
   - **سوبر أدمن = المطور = المالك**. الوحيد الذي تظهر له لوحة تحكم المطور.
   - **مسئول سنتر** (صاحب السنتر/المدرس): يدير الطلاب والمجموعات والحضور والمدفوعات والإعلانات والدرجات داخل سنتره فقط.
   - **طالب**: يرى بياناته فقط (حضوره، درجاته، مدفوعاته، إعلانات سنتره).
4. **عزل كامل بين المستخدمين بعمود `center_id`** — سنتر لا يرى بيانات سنتر آخر إطلاقاً، والطالب لا يرى إلا سجلاته هو.
5. **كود السنتر**: يختاره صاحب السنتر عند التسجيل من 3–8 حروف أو أرقام (إنجليزي/عربي/مختلط)، **ثابت لا يتغير** و**فريد على مستوى النظام**؛ تظهر له رسالة «الكود غير متاح» إن اختار كوداً مأخوذاً، وتنبيه مسبق أن الكود ثابت وفريد.
6. **تسجيل الطالب يتم بكود السنتر مرة واحدة فقط** — عند أول تسجيل يكتب الكود فيرى اسم سنتره للتأكيد، ويُربط حسابه بـ `center_id` تلقائياً؛ **عند تسجيل الدخول لاحقاً لا يُطلب الكود أبداً** (بريد + كلمة مرور فقط).
7. **الحسابات فريدة عالمياً**: البريد ورقم الهاتف لا يتكرران في النظام كله **حتى لو اختلف السنتر** — رسالة واضحة «الإيميل مستخدم» / «الرقم مستخدم».
8. **الصفحة الرئيسية ترحيبية** فيها أزرار: إنشاء حساب سنتر جديد · تسجيل طالب جديد · تسجيل دخول مسئول سنتر · تسجيل دخول طالب · حول التطبيق.
9. **لوحة تحكم المطور مخفية تماماً عن الجميع** — تُفتح بضغطة مطوّلة (٣ ثوانٍ) على شعار التطبيق في صفحة الترحيب + تسجيل دخول حساب المطور.
10. **من لوحة المطور**: التحكم في روابط قاعدة البيانات لجميع العملاء · التحكم في اشتراكات السناتر (شهري/سنوي/مخصص، تفعيل/تجديد/إيقاف) · إيقاف/تفعيل أي سنتر · تعديل محتوى صفحة «حول التطبيق» وبيانات التواصل ورسالة عامة تصل للجميع فوراً.
11. **مفاتيح قاعدة البيانات عبر كلاود فلير كوسيط**: التطبيق يسأل كلاود فلير عن مفاتيح القاعدة عند كل تشغيل؛ لتغيير القاعدة (Supabase أو أي منصة أخرى) يعدّل المالك العامل فقط فتتبعه كل الأجهزة تلقائياً.
12. **تحديث التطبيق ذاتياً عبر كلاود فلير**: المالك يرفع APK أحدث ويزيد رقمه؛ التطبيق يكتشف الإصدار الأعلى، يحمّله ويثبّته فوق القديم (مع وضع إجباري اختياري `force_update`).

---

## 3) الخريطة المعمارية الكاملة

```
┌──────────────────────────┐     ┌──────────────────────────┐
│      تطبيق الجوال        │     │      موقع الويب          │
│  (Expo / React Native)   │     │  (Next.js — سيُرقَّى)    │
└───────────┬──────────────┘     └───────────┬──────────────┘
            │                                │
            │  ① عند كل إقلاع:               │
            │  GET <configUrl>               │ يتصل مباشرة
            ▼                                │ بـ Supabase
┌──────────────────────────┐                 │ (بمفاتيحه الخاصة)
│   عامل Cloudflare Worker │                 │
│  (cloudflare/worker.js)  │                 │
│  /config يرد:            │                 │
│   • database: url+key    │                 │
│   • update: version/apk  │                 │
└───────────┬──────────────┘                 │
            │ ② يبني عميل Supabase           │
            │   بالمفاتيح المستلمة           │
            ▼                                ▼
            ┌──────────────────────────────────────────────┐
            │   Supabase (PostgreSQL + Auth + RLS)         │
            │   نفس المشروع = نفس الجداول = بيانات متطابقة │
            │   العزل والأمان على الخادم عبر RLS           │
            └──────────────────────────────────────────────┘

التحديث الذاتي: APK ← يُرفع على Cloudflare R2 ← رابطه في UPDATE.apk_url بالعامل
```

**مبدأ حاسم**: التطبيق نفسه لا يخزن أي إعدادات حساسة. المفاتيح تأتيه من الخارج (كلاود فلير)، والأمان الحقيقي كله في RLS على قاعدة البيانات — فمفتاح anon الظاهر لأي عميل لا يمنحه شيئاً تمنعه السياسات.

---

## 4) نموذج تعدد السناتر والصلاحيات

### الجداول الجديدة الأربعة (أنشأها مخططنا)
| الجدول | الغرض | أعمدة مفتاحية |
|---|---|---|
| `centers` | السناتر | `id` uuid · `name` · `code` (فريد بـ upper، ثابت) · `owner_name/email/phone` · `status` ('active'/'suspended') |
| `profiles` | تربط كل مستخدم Auth بدوره وسنتره | `id`=auth.uid · `role` ('super_admin'/'center_admin'/'student') · `center_id` · `student_id` (نص، لربط الطالب بسجله) · `full_name` · `email` (فريد) · `phone` (فريد) · `is_active` |
| `center_subscriptions` | اشتراكات السناتر | `center_id` · `plan_type` ('monthly'/'yearly'/'custom') · `starts_on/ends_on` · `status` ('active'/'expired'/'suspended') |
| `app_config` | إعدادات عامة (key/value jsonb) | `'public_config'` = محتوى «حول التطبيق»+الرسالة (قراءة عامة، كتابة مطور فقط) · `'legacy_admins'` = القائمة البيضاء (راجع القسم 12) |

### جداول الموقع القديم الموسَّعة (نفس الأسماء — مشتركة معه)
`grades` · `groups` · `students` (+`guardian_phone`) · `dues` · `payments` · `sessions` · `attendance` · `announcements` · `manual_grades` · `exams` · `honorees` · `shared_files` · `important_links` — كلها أضيف لها `center_id UUID REFERENCES centers(id)` (الصفوف القديمة تبقى NULL وتخص النظام القديم فقط).

### قواعد العزل (RLS) المعتمدة
- **دوال مساعدة** `SECURITY DEFINER`: `my_role()` · `my_center_id()` · `my_student_id()` · `is_legacy_admin()` · `center_is_active(cid)` · `admin_owns_center(cid)` — مالكها postgres فيتجاوز RLS داخلياً بلا حلقات مفرغة.
- **الطالب**: قراءة فقط ونطاقها `student_id = my_student_id()` في (students/dues/payments/attendance/manual_grades)، وبيانات سنتره العامة `center_id = my_center_id()` في (grades/groups/sessions/announcements). لا كتابة إطلاقاً.
- **مسئول السنتر**: كامل على صفوف `center_id = my_center_id()`؛ والكتابة مشروطة أيضاً بـ `center_is_active(center_id)` (سنتر موقوف = قراءة فقط خادمياً).
- **سوبر أدمن**: كامل على كل شيء.
- **النظام القديم (legacy)**: كامل على كل شيء + الصفوف ذات `center_id IS NULL` (راجع القسم 12).
- **الزائر (anon)**: يقرأ فقط `app_config` صف `public_config` + الصفوف القديمة `center_id IS NULL` في (announcements/grades/groups/honorees/shared_files/important_links) — لتبقى الصفحة العامة للموقع القديم حية؛ وينفذ فقط دالتي `lookup_center_by_code` و`check_registration_availability`.

### دوال RPC (تُستدعى من التطبيق)
| الدالة | الأذونات | الوظيفة |
|---|---|---|
| `lookup_center_by_code(text)` | anon+auth | كود → `{id,name,owner_name}` لسنتر **فعّال** فقط (للتحقق قبل التسجيل + فحص توفر الكود: رد موجود = مأخوذ) |
| `check_registration_availability(email,phone)` | anon+auth | `{email_taken, phone_taken}` — لرسائل «مستخدم من قبل» قبل إنشاء الحساب |
| `complete_center_registration(name,code,owner,phone)` | auth | في معاملة واحدة: ينشئ centers+profiles+اشتراك تجريبي 30 يوم؛ أخطاء `center_code_taken`/`phone_taken`/`already_registered`/`invalid_code_format` |
| `complete_student_registration(center_id,name,phone,guardian)` | auth | يتحقق أن السنتر فعّال، ينشئ students+profiles ويربطهما؛ أخطاء `center_not_found`/`center_suspended`/`phone_taken` |
| `get_my_subscription()` | auth | ملخص حالة اشتراك سنتر المستخدم: `{status, plan_type, ends_on, days_left, center_status}` (سوبر أدمن → active دائماً) |

### المشغلات (Triggers)
- `trg_guard_center_code` على centers: **الكود وstatus لا يغيّرهما إلا super_admin أو legacy** (`code_is_fixed` / `status_managed_by_developer`) — هذه إصلاح لثغرة: مسئول سنتر كان بإمكانه إعادة تفعيل نفسه.
- `trg_guard_profile_identity` على profiles: `role/center_id/student_id/is_active` لا يغيّرها المستخدم لنفسه (`identity_protected`).
- `trg_sync_group_count` على students: مزامنة `groups.students_count` تلقائياً (عدّ النشطين فقط).

### قيود الفرادة (تحقيق متطلبات المالك)
- `centers_code_unique` على `upper(code)` → «الكود غير متاح»
- `profiles_email_unique` على `lower(email)` + فريدية Auth نفسها → «الإيميل مستخدم»
- `profiles_phone_unique` على `phone` → «الرقم مستخدم»

---

## 5) قاعدة البيانات بالتفصيل

ملف المخطط الوحيد الذي يُشغَّل: **`supabase/android_multitenant_schema.sql`**
- آمن لإعادة التشغيل (IF NOT EXISTS / OR REPLACE / DROP IF EXISTS) داخل معاملة واحدة.
- لا يحذف بيانات ولا يكسر الموقع القديم.
- يشتمل على INSERT مبدئي لصف `app_config/public_config` (محتوى «حول التطبيق» الافتراضي) ولبذر `legacy_admins` تلقائياً من حسابات `auth.users` الموجودة لحظة الترحيل.
- **لا تُعِد تشغيل `schema.sql` الخاص بالموقع القديم بعده** — يعيد السياسات الواسعة `USING (true)` ويهدّم العزل (مذكور كتحذير في README أيضاً).

إعدادات Auth المطلوبة في مشروع Supabase: تفعيل «Allow new users to sign up» + إيقاف «Confirm email» (الدخول فوري بعد التسجيل).

إنشاء حساب المطور: مستخدم من لوحة Authentication ثم:
```sql
INSERT INTO public.profiles (id, role, full_name, email)
VALUES ('<UID>', 'super_admin', 'المطور', 'dev@mail.com');
```

---

## 6) نظام كلاود فلير (الوسيط المركزي)

### العامل — `cloudflare/worker.js`
قسّمان يعدّلهما المالك من لوحة Cloudflare ثم Deploy:
- `DATABASE = { provider, url, anon_key }` — مفاتيح قاعدة البيانات الحالية (أي منصة).
- `UPDATE = { latest_version, version_code, apk_url, force_update, changelog }` — إعلان آخر إصدار.

المسارات: `/config` (و`/`) database+update · `/version` · `/database` · ترويسات CORS مفتوحة + `Cache-Control: max-age=60` (تغييراته تصل خلال دقيقة).

### سلسلة التهيئة في التطبيق — `src/lib/supabase.ts#initSupabase()`
بالترتيب: **① مفاتيح يدوية للمطور (override محفوظة على الجهاز) → ② ردّ كلاود فلير (مع رجوع تلقائي لآخر رد ناجح مخزّن `mrcenter.remote.config` عند انقطاع الشبكة) → ③ آخر مفاتيح عاملة مخزنة (`mrcenter.cfg.cache`) → ④ المدمجة من `app.json extra.supabaseUrl/supabaseAnonKey`**. انعدام الكل → تظهر شاشة «إعداد الاتصال لأول مرة» (`src/components/ConnectionSetup.tsx`) ليدخل المطور المفاتيح يدوياً.

الرابط الثابت الوحيد داخل التطبيق: `app.json → extra.configUrl` = رابط `/config` للعامل. **تغييره يستلزم إصداراً جديداً من التطبيق** — لذا العامل يعيش مع المنتج كله (لا تحذفه أبداً).

### التحديث الذاتي — `src/lib/updater.ts` + `src/components/UpdateManager.tsx`
- المنطق: يقارن `version_code` (أساسي) أو `latest_version` semver (احتياطي) بالحالي (`Application.nativeBuildVersion/nativeApplicationVersion`).
- عند الأحدث: نافذة «ما الجديد» ← «تحديث وتثبيت الآن» ← تحميل بشريط تقدم (`FileSystem.createDownloadResumable`) ← `getContentUriAsync` ← `IntentLauncher` بـ `VIEW` + `application/vnd.android.package-archive` — مثبّت النظام يثبّت فوق القديم (نفس شهادة التوقيع شرط).
- `force_update: true` = نافذة غير قابلة للإغلاق تحظر التطبيق.
- يتخطى تلقائياً: `__DEV__` و Expo Go وغير أندرويد.
- إذن مضاف: `REQUEST_INSTALL_PACKAGES` في app.json. ملفات APK تُستضاف على **R2** باسم ثابت `mr-center.apk` (الدليل الكامل: `cloudflare/README.md`).

---

## 7) بنية كود التطبيق ملفاً ملفاً

```
index.ts                  فرض RTL ثم expo-router/entry
app.json                  اسم/حزمة com.mrcenter.app · صلاحيات · extra.configUrl (+مفاتيح مدمجة اختيارية)
eas.json                  preview=APK داخلي · production=AAB متجر مع autoIncrement

app/  (شاشات expo-router — مجلدات (x) مجموعات بلا مسار)
├─ _layout.tsx            الجذر: SessionProvider + RouterGuard (التوجيه بالصلاحية والاشتراك) + <UpdateManager/>
├─ index.tsx              الترحيب: 4 أزرار + حول التطبيق + بوابة المطور (ضغطة مطوّلة على الشعار) + ConnectionSetup عند غياب الاتصال
├─ about.tsx              «حول التطبيق» — محتواها من app_config/public_config
├─ blocked.tsx            حظر الاشتراك المنتهي/الموقوف (بيانات الحالة + واتساب المطور + إعادة فحص)
├─ auth/
│  ├─ login-admin.tsx / login-student.tsx   غلاف يمرر expectedRole لـ LoginForm
│  ├─ register-center.tsx                   تسجيل سنتر: تحقق لحظي من توفر الكود + تنبيه «ثابت وفريد» + checkAvailability
│  └─ register-student.tsx                  خطوتان: كود السنتر (بطاقة تأكيد باسم السنتر) ثم البيانات
├─ developer/             حماية Layout: غير super_admin ← بوابة دخول خاصة/«منطقة محظورة»
│  ├─ index.tsx           لوحة المطور: إحصائيات النظام + روابط الأقسام
│  ├─ connection.tsx      مفاتيح الربط: عرض المصدر (كلاود فلير/يدوي/مخزن/مدمج) + مفاتيح يدوية للتجربة + معلومات التحديث
│  ├─ centers.tsx         كل السناتر: الكود/المالك/عدد الطلاب/آخر اشتراك + إيقاف/تفعيل
│  ├─ subscriptions.tsx   اختيار سنتر ← تفعيل/تجديد (شهرية/سنوية/مخصصة بمدد) أو إيقاف
│  └─ app-info.tsx        محرر «حول التطبيق»+بيانات التواصل+الرسالة العامة → app_config
├─ (admin)/               تبويبات مسئول السنتر + حارس دور + شارة اقتراب انتهاء الاشتراك
│  ├─ dashboard.tsx       ترحيب متدرج + كود السنتر + 6 إحصائيات + إجراءات سريعة + آخر الإعلانات
│  ├─ students.tsx        بحث + إضافة/تعديل/حذف (مودال) + ?add=1 يفتح النموذج تلقائياً
│  ├─ groups.tsx          CRUD + منتقي الأيام + المواعيد + الرسوم الشهرية
│  ├─ attendance.tsx      مجموعة+تاريخ ← حصة تلقائية ← تبديل حاضر/متأخر/غائب لكل طالب ← حفظ جماعي (upsert)
│  ├─ more.tsx            قائمة الأقسام + بطاقة كود السنتر مع مشاركة
│  ├─ payments.tsx        توليد مستحقات شهرية لمجموعة (يتجاوز المكرر) + معلق/مسدد + زر تحصيل لملف الطالب
│  ├─ announcements.tsx   نشر/تثبيت/حذف إعلانات السنتر
│  ├─ grades-list.tsx     الصفوف الدراسية: إضافة سريعة + حذف
│  ├─ admin-settings.tsx  بيانات السنتر + حالة الاشتراك وأيامه + تسجيل خروج
│  └─ student/[id].tsx    ملف الطالب الشامل: بيانات+تغيير مجموعة+مستحقات+درجات+دفعات (?pay=<dueId> يفتح التحصيل)
└─ (student)/             تبويبات الطالب + حارس دور
   ├─ home.tsx            ترحيب + بطاقة «مجموعتي» (أيام/مواعيد) + ملخص + إعلانات السنتر
   ├─ my-attendance.tsx   سجل الحضور بحالات ملونة
   ├─ my-grades.tsx       الدرجات + المتوسط العام بالنسبة المئوية
   ├─ my-payments.tsx     المعلق والمسدد بإجماليات
   └─ profile.tsx         بياناته + سنته وكودها + تسجيل خروج

src/
├─ theme.ts               ألوان/تدرجات/أنصاف أقطار/ظلال موحدة (داكن بنفسجي-نيلي-سماوي)
├─ components/
│  ├─ layout.tsx          GradientScreen · (Back)Header · KeyboardScreen
│  ├─ controls.tsx        AppButton (متدرج 6 أنماط) · AppInput · Card · StatCard · ListItem · EmptyState · LoadingView · SectionTitle
│  ├─ pickers.tsx         OptionPicker (شيت سفلي) · DaysPicker · FormMessage
│  ├─ LoginForm.tsx       دخول موحد بتحقق الدور بعد الدخول (+نسيت كلمة المرور)
│  ├─ ConnectionSetup.tsx شاشة إدخال المفاتيح لأول مرة (غياب كل المصادر)
│  └─ UpdateManager.tsx   نافذة التحديث: تغييرات + شريط تقدم + وضع إجباري
└─ lib/
   ├─ types.ts            كل الأنواع مطابقة لأعمدة الجداول
   ├─ utils.ts            دوال نقية قابلة للاختبار: uuid · normalizePhone (أرقام عربية↔لاتينية) ·
   │                      normalizeCenterCode/isValidCenterCode (3–8 عربي/إنجليزي/أرقام) · isValidEmail/Phone ·
   │                      isValidSupabaseUrl · dbConfigFromRemote · compareVersions · تنسيق التواريخ/الأموال · arabicError
   ├─ config.ts           مصادر الإعدادات + AsyncStorage keys: mrcenter.cfg.override/.cache + mrcenter.remote.config + fetchRemoteConfig (timeout 9s)
   ├─ supabase.ts         بناء العميل الديناميكي + initSupabase (سلسلة الأولوية) + fetchPublicConfig
   ├─ session.tsx         SessionProvider: تهيئة الاتصال + جلسة Auth + profile + subscription + refresh/signOut
   ├─ api.ts              كل الاستعلامات (تسجيل، إدارة سنتر، حضور، ماليات، إعلانات، طالب، لوحة مطور) — كلها تبيع `center_id`/RLS
   └─ updater.ts          checkForUpdate · downloadAndInstall · حُراس البيئة

scripts/                  جناح الفحص (القسم 10)
cloudflare/               العامل + دليل إعداده
supabase/                 المخطط الوحيد
assets/icon.png           الشعار (قبعة تخرج بحرف M بتدرج بنفسجي-سماوي على كحلي)
```

**اصطلاحات الكود**: مسارات بنادلة `@/*`→`src/*` (مفعّلة افتراضياً في Expo SDK ≥50) · استعلامات كائن supabase عبر `getSupabase()` فقط · لا حالة عالمية خارج SessionProvider · الأدوار تُستنتج من `profiles.role` ولا يثق العميل بمدخلات نفسه · كل أخطاء الشبكة تُعرّب عبر `arabicError()`.

---

## 8) تدفقات الاستخدام الحرجة

### تسجيل مسئول سنتر جديد
واجهة تملأ البيانات ← فحص لحظي `lookup_center_by_code` للكود (موجود = مأخوذ) ← `check_registration_availability` للبريد/الهاتف ← `auth.signUp` ← `complete_center_registration` (معاملة خادمية: سنتر+ملف+اشتراك تجريبي 30 يوم) ← `refresh()` ← الحارس يوجهه لـ `/dashboard`.

### تسجيل طالب جديد
الخطوة 1: يرد الكود ← `lookup_center_by_code` ← بطاقة «تأكد أن هذا سنترك» باسم السنتر ومالكه ← الخطوة 2: بياناته ← فحص التوفر ← `signUp` ← `complete_student_registration` (ينشئ طالباً+ملفاً مربوطين) ← الحارس يوجهه لـ `/home`. **الكود لا يسأل عنه بعدها أبداً**.

### الدخول
بريد+كلمة 👮‍♂️ ← `signInWithPassword` ← LoginForm يفحص `profiles.role` الفعلي و`is_active` ويرفض الخلط (طالب في شاشة مسئول والعكس مع `signOut`) ← الحارس يوجه بالصلاحية، ويفرض `/blocked` لو الاشتراك `suspended/expired` (سوبر أدمن معفى).

### تغيير قاعدة البيانات لكل العملاء
المالك يعدّل `DATABASE` في العامل ← Deploy ← عند إقلاع كل جهاز: `fetchRemoteConfig` يجلب الجديد ← يعيد بناء العميل ويخزنه ← ولا يفقد العمل بلا إنترنت (آخر رد مخزن).

### إصدار تحديث
`version`+`versionCode` في app.json ← بناء APK (EAS) ← رفعه لـ R2 ← تحديث `UPDATE` في العامل ← كل جهاز عند إقلاعه يرى النافذة ويثبّت فوق القديم.

### RouterGuard (app/_layout.tsx) — نقطة حساسة
`useSegments` يعيد **شاملاً مجلدات التجميع** `(admin)`/`(student)` — لذا يُستبعد كل ما يبدأ بـ `(` قبل حساب الجذر (كان خطأً فادحاً لو نُسِي: حلقة إعادة توجيه لا نهائية). المسارات الفعلية بالأسماء: student→/home · admin→/dashboard · super_admin→/developer.

---

## 9) الثوابت التي يُمنع كسرها

قبل أي تعديل، تحقق ألا تكسر هذه (يحميها جناح `npm test` آلياً):

1. **لا سياسات RLS واسعة** `USING (true)`/`WITH CHECK (true)` أبداً في مخططنا.
2. **العزل خادمي لا عميلي**: لا تعتمد على إخفاء بيانات في الواجهة. أي كيان جديد متعدد السناتر = عمود `center_id` + سياساته + تسجيله في `scripts/audit-sql.mjs`.
3. **كود السنتر ثابت وفريد**، وstatus يديره super_admin — الحماية بمشغّلات خادمية، لا تتجاوزها.
4. **الطالب لا يكتب شيئاً**؛ يقرأ نفسه فقط عبر `my_student_id()`.
5. **القائمة البيضاء `legacy_admins`** هي الطريقة الوحيدة لصلاحية «النظام القديم» — **ممنوع** إرجاع منطق «بلا ملف = أدمن» (كانت ثغرة سُدّت).
6. **البريد/الهاتف فريدان عالمياً** — الحفاظ على الفهارس الفريدة وعلى الفحص الاستباقي قبل signUp.
7. **الأسماء المشتركة مع الموقع** (جداول وأعمدته النصية id/created_at) لا تُغيَّر نوعها؛ الزيادات بأعمدة جديدة فقط.
8. **لا تُعِد تشغيل مخطط الموقع القديم** بعد مخططنا أبداً على نفس المشروع.
9. `extra.configUrl` هو الثابت الوحيد — تغييره = إصدار جديد إجباري للعملاء.
10. RTL والعربية في كل واجهة جديدة، واتجاهات النصوص صريحة (`textAlign:'right'`, `writingDirection:'rtl'`, وحقول الأكواد/البريد/الأرقام LTR).
11. عند تغيير رقم الإصدار: حدّث `app.json` (version+versionCode) **والعامل معاً** — يوجد اختبار توافق آلي.
12. عند أي تعديل: شغّل `npm test` و`npm run typecheck` وتحقق من `expo export` قبل الرفع.

---

## 10) الاختبارات والتحقق

`npm test` يشغّل أربعة أجنحة بلا أي خادم حقيقي:
| الملف | الفحوص |
|---|---|
| `scripts/test-utils.mjs` | 45 اختبار منطق نقي (يُجمّع utils.ts بـ tsc مؤقتاً): التطبيع، صيغ الكود، مقارنة الإصدارات، استخراج إعدادات كلاود فلير، تعريب الأخطاء |
| `scripts/test-worker.mjs` | 19 اختباراً للعامل بمحاكاة Request/Response حقيقية في Node (المسارات، البنية، CORS، الكاش، توافق الإصدارات مع app.json) |
| `scripts/audit-sql.mjs` | 65 فحصاً أمنياً ثابتاً للمخطط (البنية، RLS بلا ثغرات، العزل، القائمة البيضاء، المشغلات، الفرادة، أذونات RPC) |
| `scripts/audit-app.mjs` | 32 فحصاً ثابتاً (الإعدادات، عدم تسريب service_role، تغطية الأدوار والحُراس، شروط التحديث الذاتي) |

+ `npm run typecheck` (صارم) + `CI=1 npx expo export --platform android` للتأكد أن الحزمة تُبنى — كلها كانت خضراء عند التسليم. **عند فشل أي فحص لا ترفع العمل**.

---

## 11) البناء والتوزيع والتحديثات

- التجربة الفورية: `npm install && npx expo start` ← Expo Go يمسح QR.
- APK مباشر: `eas build --platform android --profile preview` · AAB للمتجر: `--profile production` (EAS يدير Keystore/الشهادة تلقائياً — نفسها دائماً، وهي شرط التحديث-فوق-القديم).
- بعد التوزيع الأول: كل التحديثات عبر R2+العامل دون إعادة تثبيت يدوي (دليل `cloudflare/README.md`).
- نسخة iOS مستقبلاً: بناء مستقل عبر EAS؛ التحديث الذاتي أندرويد فقط (iOS عبر App Store فقط).

---

## 12) علاقة التطبيق بموقع الويب القديم

موقع `centerpuplish` (Next.js) كان **أحادي المستأجر**: حساب واحد لمدرس + بوابة طلاب بمصادقة خاصة (student_accounts + RPC، وليس Supabase Auth). قرارات التوافق:

- **نفس الجداول والأعمدة** — اكتفينا بإضافة `center_id`، فبيانات الموقع الحالية (center_id=NULL) سليمة وتخصه، وبيانات التطبيق تحمل سنترها.
- السياسات القديمة الواسعة حُذفت واستُبدلت بعزل كامل، مع سياسة `legacy` تمنح **القائمة البيضاء فقط** وصولاً كاملاً — فالموقع القديم يستمر بالعمل دون أي تعديل.
- الصفحة العامة للموقع (anon) تقرأ فقط الصفوف القديمة NULL — بيانات السناتر الجديدة غير مكشوفة للزائر.
- **ترقية الموقع لاحقاً** (وعد بها المالك): هدفها جعل استعلاماته تملأ/تصفّي بـ `center_id` واعتماد Auth+profiles بدل بوابة الطلاب الخاصة — عندها تُقرأ كل بيانات التطبيق فوراً لأنها في نفس الجداول أصلاً. بيانات الموقع ما قبل الترقية يمكن إسنادها لسنتر بتحديث `center_id` يدوياً من SQL.
- إضافة حساب قديم للقائمة البيضاء لاحقاً:
  `UPDATE public.app_config SET value = value || '"<UID>"'::jsonb WHERE key='legacy_admins';`

---

## 13) بيئة العمل وخبرات عملية مهمة

- **Node 22 / npm متاحان**؛ سجل npm يعمل؛ **واجهة expo API (npx expo install / doctor) محجوبة شبكياً** — علّق الإصدارات يدوياً من `node_modules/expo/bundledNativeModules.json` (مطابقة SDK 52) كما فُعل عند تثبيت expo-asset/application/file-system/intent-launcher.
- **لا Android SDK ولا Java ولا Flutter** في البيئة — التحقق من «البناء» يكون بـ `tsc --noEmit` + `npx expo export --platform android` (Metro bundle) والاختبارات الساكنة؛ بناء APK الحقيقي عند المستخدم عبر EAS.
- **`node_modules` لا تُحفَظ بين الجلسات** — ابدأ كل جلسة بـ `npm install` (~دقيقة).
- **حالة git قد تُصفَّر بين الجلسات**: الحفاظ على العمل بالكوميت والدفع الفوري لفرع `arena/01a08697-mr-center-android`؛ عند رفض الدفع غير المتقدم: `git fetch` ثم rebase على FETCH_HEAD وحل التعارضات بالأخذ من كوميتك النهائي (`git checkout <sha> -- <files>` ثم `GIT_EDITOR=true git rebase --continue`).
- فرع العمل ثابت: `arena/01a08697-mr-center-android` — لا تُنشئ فروعاً أخرى، والدفع فقط إليها.

---

## 14) قيود معروفة وخطط مستقبلية

- الاختبارات/الدرجات الإلكترونية المتقدمة (exams, online_exam_sessions, المحاولات) — بقيت للموقع ولم تُنقل للتطبيق بعد (سياساتها مقيّدة بالفعل: لا يقرأها الطالب خاماً).
- بوابة الطلاب القديمة (student_accounts, registration_requests, inquiries, student_history...) بقيت كما هي دون استخدام من التطبيق — يمكن توحيدها عند ترقية الموقع.
- الربط اليدوي لطالب أضافه المسئول يدوياً بحساب يُسجَّل لاحقاً بنفس الهاتف — غير مُنفَّذ (التسجيل ينشئ سجلاً جديداً مربوطاً).
- إشعارات Push وتقارير مطوّلة وطباعة الجداول — مستقبلاً.
- iOS: تحديثات نظام التشغيل تمنع التثبيت الذاتي — عبر المتجر.
- دالة `check_registration_availability` للزائر كاشفة جزئياً لتكرار البريد/الهاتف — مقبولة تصميمياً (مطلوبة لوضوح الرسائل) ومحمولة بحدود Supabase للمعدل.

---

## 15) قاموس مصطلحات المالك

| يقصد | المعنى عندنا |
|---|---|
| «السنتر» | المركز التعليمي/المدرس صاحب الحساب الإداري (`centers`) |
| «كود السنتر» | `centers.code` — 3–8 حروف/أرقام، ثابت وفريد، به ينضم الطلاب |
| «سنتر اي دي / العزل» | `center_id` + سياسات RLS |
| «سوبر أدمن / المطور» | role='super_admin' — المالك نفسه |
| «مسئول السنتر / المدرس» | role='center_admin' |
| «مفاتيح الربط» | url + anon_key لقاعدة البيانات |
| «كلاود فلير» | العامل في `cloudflare/worker.js` (+R2 لملفات APK) |
| «الاشتراك» | `center_subscriptions` — يتحكم به المطور وحده |
| «الموقع القديم/النظام القديم» | مشروع centerpuplish وحسابه الأصلي (القائمة البيضاء) |
| «تحديث التطبيق من كلاود فلير» | التحديث الذاتي: فحص الإصدار ← تحميل APK ← تثبيت فوق القديم |

---

## 16) قائمة فحص قبل التسليم

أي مساهمة جديدة لا تُسلَّم إلا بعد:
- [ ] `npm test` — الأجنحة الأربعة خضراء (وأضِف فحوصاً لما أضفته).
- [ ] `npm run typecheck` — صفر أخطاء.
- [ ] `CI=1 npx expo export --platform android` — الحزمة تُبنى.
- [ ] مراجعة القسم 9 (الممنوعات) بنداً بنداً.
- [ ] العربية RTL في كل واجهة، ورسائل الأخطاء معرّبة.
- [ ] README/الوثائق حُدّثت لو تغيّر سلوك أو إعداد.
- [ ] كوميت واضح بالعربية + دفع لفرع العمل نفسه.

> هذه الوثيقة حيّة: حدّثها عند أي تغيير معماري — حتى يظل من يعمل بعدك فاهماً «كل شيء حرفياً».
